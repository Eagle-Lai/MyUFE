# MyprojectDemo 项目说明文档

## 1. 项目概述

**MyprojectDemo** 是一个基于 **Unity 2020.3.17f1** + **UFE（Universal Fighting Engine）2.x** 的 2D 横版格斗游戏 Demo。

- **游戏名称**：UFE Training Room
- **引擎**：Unity 2020.3.17f1（`ProjectSettings/ProjectVersion.txt`）
- **核心插件**：UFE 2.x（`Assets/UFE/`），这是一个完整的 2D 格斗游戏框架（街霸/拳皇类）
- **运行平台**：PC / Mac / Linux（Standalone），Demo 以 PC 键盘为默认操作方式
- **演示内容**：主菜单、对战（人机 / 双人 / 观战）、故事模式、训练模式、4 个可选角色、1 个对战场景（Training Room）、完整战斗系统（血条/能量槽/回合/必杀技/连招）

> 项目此前存在大量编译错误，根源是 UFE 插件使用了 Unity 2018.2 移除的旧 API（GUIText/GUITexture、旧 UNET 网络、MovieTexture 等）。现已通过新建兼容层 `Assets/UFE Addons/Compatibility/UFELegacyAPICompat.cs` 全部修复，**当前编译无错误**，可直接运行。

---

## 2. 项目结构

```
g:\MyprojectDemo\
├─ Assets\
│  ├─ Scenes\                     # 空场景（SampleScene，未使用）
│  ├─ StreamingAssets\
│  │  ├─ UFEIntroShort.mp4        # 片头视频资源（VideoIntroScreen 用）
│  │  └─ UFEIntroShort.ogv
│  ├─ UFE\                        # UFE 插件本体
│  │  ├─ Engine\                  # 引擎核心
│  │  │  ├─ Scripts\              # 全部核心代码（218 个 .cs）
│  │  │  │  ├─ Manager\UFE.cs     # ★ 全局管理器（入口）
│  │  │  │  ├─ Physics\ControlsScript.cs   # 战斗物理/输入处理核心
│  │  │  │  ├─ Input\             # 输入系统（InputController 等）
│  │  │  │  ├─ Definitions\       # 配置类（GlobalInfo/CharacterInfo/MoveInfo）
│  │  │  │  └─ UI\                # 界面系统（UFEScreen 基类 + Templates 模板）
│  │  │  ├─ Editor\               # 编辑器工具（UFE 配置编辑器）
│  │  │  └─ ProjectSettings\UFEInput.asset  # UFE 输入轴向定义
│  │  ├─ Demo\                    # ★ 演示内容
│  │  │  ├─ TrainingRoom.unity    # ★ 唯一 Build 场景（入口场景）
│  │  │  ├─ Config.asset          # ★ 演示全局配置（核心配置文件）
│  │  │  ├─ Characters\           # 4 个角色（Ethan/Mike/Mecanim Bot/Robot Kyle）
│  │  │  ├─ GUI\UI_Prefabs\       # 全部界面预制体（28 个）
│  │  │  ├─ Stages\               # 场景（Training Room）
│  │  │  ├─ Sounds\ / Particles\ / AIInstructions\ / Resources\
│  │  ├─ UFE_Config.asset         # UFE 全局配置（Demo 用 Config.asset）
│  │  ├─ Main Camera.prefab
│  │  └─ Readme.txt / Installation.pdf
│  └─ UFE Addons\Compatibility\   # ★ 兼容层（修复旧 API 的关键，勿删）
├─ ProjectSettings\
│  ├─ InputManager.asset          # ★ Unity 输入轴（P1/P2 键位定义）
│  └─ EditorBuildSettings.asset   # Build 场景列表
└─ Packages\
```

### 关键文件说明

| 文件 | 作用 |
|---|---|
| `Assets/UFE/Demo/TrainingRoom.unity` | **入口场景**，仅含 `UFE Manager`（UFE.cs + EventSystem）和 `Camera`，运行时会动态创建全部系统 |
| `Assets/UFE/Demo/Config.asset` | **全局配置**（游戏名、回合数、计时、GUI 预制体引用、AI、训练模式参数等） |
| `ProjectSettings/InputManager.asset` | **键盘按键映射**（P1KeyboardHorizontal、P1Button1~6、P1Start 等轴） |
| `Assets/UFE Addons/Compatibility/UFELegacyAPICompat.cs` | 旧 API 兼容层（GUIText/Network/MovieTexture 等 Compat 类型） |

---

## 3. 技术架构

- **入口/全局管理器**：`UFE.cs`（挂在场景 `UFE Manager` 上）。`Awake()` 读取 `Config.asset`，根据 `debugOptions.startGameImmediately` 决定启动后进入片头（当前配置 `= 0`，走片头）还是直接开打。
- **配置体系**：`GlobalInfo.cs` 定义 `GlobalInfo`/`CharacterInfo`/`MoveInfo` 等配置类，运行时从 `.asset` 文件加载。
- **输入系统**：`InputController.cs` 按 Unity Input Manager 轴名（`P1Button1` 等）读取输入 → 转成 `InputEvents` → 交给 `ControlsScript.DoFixedUpdate()`。
- **战斗物理**：`ControlsScript.cs`（固定帧率），负责移动、拳脚判定、按钮序列识别（必杀技/连招）、防守、投掷、HitBox 判定。
- **UI 系统**：`UFEScreen.cs` 基类 → `Templates/Default*Screen.cs` 模板实现。所有界面都是挂 `UFEScreen` 的预制体，由 `UFE.cs` 动态加载显示/隐藏。
- **AI 系统**：`AIInstructions` 资产 + `FuzzyAI` 引擎（Config 中 `aiOptions.engine: 1`），难度 0~5，当前 `selectedDifficultyLevel: 2`（中等）。
- **网络**：旧 UNET 已被 Unity 移除，UFE 通过反射检测 `UnetHighLevelMultiplayerAPI` 不存在时自动降级为 `NullMultiplayerAPI`。因此 **Online / Bluetooth 菜单不可用**（灰显）。

---

## 4. 如何运行项目

### 方式一：Unity 编辑器直接运行（推荐）

1. 使用 **Unity Hub** 打开项目目录 `g:\MyprojectDemo`（需已安装 **Unity 2020.3.17f1**，首次打开会自动重新导入资源并编译）。
2. 在 Unity 中打开入口场景：
   - **File → Open Scene** → 选择 `Assets/UFE/Demo/TrainingRoom.unity`
   - （该场景是 Build Settings 中唯一的场景，也可直接双击打开）
3. 点击编辑器顶部工具栏的 **▶ Play** 按钮运行。
4. 运行后自动进入游戏：片头 → 主菜单，即可开始操作（见第 5 节）。

### 方式二：构建成独立程序

1. **File → Build Settings** → 确认场景列表只包含 `Assets/UFE/Demo/TrainingRoom.unity`。
2. Platform 选择 **PC, Mac & Linux Standalone** → 点击 **Build**。
3. 运行生成的 `.exe`（Windows）即可。

> 提示：片头引用的 `TextureIntroScreen` 是纹理片头，不需联网；`StreamingAssets` 里的 mp4/ogv 是备用的视频片头资源（`VideoIntroScreen`），当前配置未使用。

---

## 5. 游戏流程与操作指南

### 5.1 整体流程

```
启动(▶Play)
   │
   ▼
片头界面 TextureIntroScreen（按任意键跳过，或等待 3 秒）
   │
   ▼
主菜单 MainMenuScreen
   ├── Versus（对战）─────────────► 选模式(PvCPU/PvP/CvC) ► 选角色 ► 选场地 ► 战斗 ► 结算
   ├── Story Mode（故事模式）──────► 选角色 ► 连续战斗（Random Fights → Boss Fight）
   ├── Training Mode（训练模式）───► 选角色 ► 战斗（生命自动回血）
   ├── Online / Bluetooth（不可用，灰显）
   ├── Options（选项）
   ├── Credits（制作人员）
   └── Exit（退出）
```

### 5.2 通用菜单操作（所有界面通用）

| 操作 | 玩家 1 | 玩家 2 | 说明 |
|---|---|---|---|
| 移动光标 | `W A S D` | `方向键 ↑↓←→` | 在按钮间移动选择 |
| 确认 / 确定 | `T`（轻拳） | `Insert`（轻拳） | 即配置中的 confirmButton = Button1 |
| 取消 / 返回 | `G`（轻脚） | `Delete`（轻脚） | 即配置中的 cancelButton = Button4 |
| 点击 | 鼠标左键 | — | 所有按钮也可直接用鼠标点击 |

> 菜单确认键就是"轻拳"键。原因：`Config.asset` 中 `confirmButton: 4`（=Button1），`cancelButton: 7`（=Button4）。

### 5.3 各界面详细操作

**① 片头（Intro）**
- 按 **任意键** 跳过动画，或等待 3 秒自动进入主菜单。

**② 主菜单（Main Menu）**——按钮列表
| 按钮 | 作用 |
|---|---|
| **Versus** | 进入对战模式选择 |
| **Story Mode** | 故事模式（固定流程连续战斗） |
| **Training Mode** | 训练模式（生命自动回血，适合练习连招） |
| **Online** | 在线对战（本环境不可用，灰显） |
| **Bluetooth** | 蓝牙对战（仅移动平台可用，PC 灰显） |
| **Options** | 游戏选项（音乐/音效/按键设置） |
| **Credits** | 制作人员名单 |
| **Exit** | 退出游戏 |

**③ 对战模式选择（Versus）**
- **Player vs CPU**：P1 用键盘操作，P2 由电脑控制（推荐新手选择）
- **Player vs Player**：双人同键盘对战
- **CPU vs CPU**：电脑观战模式
- **Back**：返回主菜单

**④ 角色选择（Character Selection）**
- P1 用 `WASD` 移动光标 + `T` 确认；P2 用 `方向键` + `Insert` 确认。
- 屏幕左右两侧大立绘实时显示双方当前选中的角色。
- 双方都确认选人后自动进入下一界面。（PvP 时两人分别选人；PvCPU 时只需 P1 选人）

**⑤ 场地选择（Stage Selection）**
- 仅一个场地：**TRAINING ROOM**。方向键选择 + 确认键确定（或鼠标点击）。

**⑥ 战斗（Battle）**
- 界面元素：左右血条、双方头像/名字、底部**能量槽（超必杀槽）**、中间回合倒计时（99 秒）、回合胜利指示（KO/Round 提示）。
- **3 局 2 胜**（`totalRounds: 3`）。
- 战斗中按 **Enter**（P1 Start）或 **Space**（P2 Start）可暂停。

**⑦ 暂停菜单（Pause）**
- **Resume Game**：继续战斗
- **Options**：音乐（Music）、音效（Sound FX）、控制说明（Controls）、更改按键（Change Controls）
- **Controls**：查看 P1/P2 完整键位表（游戏内自带说明）
- **Special Moves**：查看本角色必杀技/连招指令表（游戏内自带）
- **Back to Menu**：返回主菜单（弹出 YES/NO 确认框）

**⑧ 战斗结算（After Battle）**
- **Repeat Battle**：与同一角色组合立即再战
- **Character Selection**：重新选角色
- **Stage Selection**：重新选场地
- **Main Menu**：返回主菜单

---

## 6. 战斗按键总表（畅玩核心）

### 玩家 1（左侧，键盘左侧区域）

| 功能 | 按键 |
|---|---|
| 移动（左右/上跳/下蹲） | `A` `D` `W` `S` |
| 轻拳 | `T` |
| 中拳 | `Y` |
| 重拳 | `U` |
| 轻脚 | `G` |
| 中脚 | `H` |
| 重脚 | `J` |
| Start（暂停/确认） | `Enter` |

### 玩家 2（右侧，键盘右侧区域）

| 功能 | 按键 |
|---|---|
| 移动 | `←` `→` `↑` `↓` |
| 轻拳 | `Insert` |
| 中拳 | `Home` |
| 重拳 | `PageUp` |
| 轻脚 | `Delete` |
| 中脚 | `End` |
| 重脚 | `PageDown` |
| Start（暂停/确认） | `Space` |

> 以上键位定义在 `ProjectSettings/InputManager.asset` 中（`P1Button1=t`、`P1Button2=y`、`P1Button3=u`、`P1Button4=g`、`P1Button5=h`、`P1Button6=j`、`P1Start=return`；P2 对应 `Insert/Home/PageUp/Delete/End/PageDown/space`）。玩家若接入手柄/摇杆，插上后即可自动映射（InputManager 中已配 `joystick button` 备用轴）。

---

## 7. 战斗玩法与出招表

### 7.1 基础操作

- **进攻**：走近对手，用拳脚按钮攻击（轻/中/重拳，轻/中/重脚）。
- **防守**：按住**后方向**（远离对手的方向，即 `A` 或 `←`）格挡。
- **移动**：`W` 跳、`S` 蹲、`A/D` 左右走。
- **连段**：轻攻击 → 中攻击 → 重攻击 依次衔接可形成连招。
- **能量槽**：攻击命中等会积攒能量，攒满后可发超必杀。

### 7.2 必杀技（游戏内暂停菜单 → Special Moves 可查看）

以下指令中"拳"指轻拳 `T` / 中拳 `Y` / 重拳 `U` 任意一个，方向以"面朝右侧"为例（面朝左侧时方向反过来）：

| 招式 | 指令（按键序列） | 备注 |
|---|---|---|
| **Fireball（波动拳）** | `↓→` + 拳（即 `S D` 快速接 `T/Y/U`） | 远程飞行道具 |
| **Dragon Punch（升龙拳）** | `→↓→` + 拳（即 `D S D` 快速接 `T/Y/U`） | 对空、无敌升龙 |
| **Super Fireball（超必杀·波动）** | `↓→↓→` + 轻拳（`S D S D` + `T`） | 需能量槽满 |
| **Super Dragon Punch（超必杀·升龙）** | `↓→↓→` + 中拳（`S D S D` + `Y`） | 需能量槽满 |

### 7.3 自定义连招（Custom Combos，可在暂停菜单 Special Moves 中查看）

| 连招 | 指令 |
|---|---|
| Combo 1 | 轻拳(`T`)、轻脚(`G`)、中脚(`H`)、重脚(`J`) |
| Combo 2 | 轻拳(`T`)、重升龙(`→↓→`+`U`) |
| Combo 3 | 蹲轻拳、蹲轻脚、蹲中拳、蹲中脚、蹲重脚 |
| Combo 4 | 蹲中脚、蹲重拳、跳起中脚、中拳、落地蹲重拳、超必杀波动 |

---

## 8. 四个角色简介

| 角色 | 资源目录 | 说明 |
|---|---|---|
| **Ethan** | `Demo/Characters/Ethan/` | 生命 1100、能量槽 1000，招式最全（含全部必杀技与连招），**推荐首选** |
| **Mike** | `Demo/Characters/Legacy_Mike/` | 传统 Legacy 动画系统角色 |
| **Mecanim Bot** | `Demo/Characters/Mecanim_Bot/` | 机械人，Mecanim 动画系统 |
| **Robot Kyle** | `Demo/Characters/Robot_Kyle/` | 机器人 Kyle |

> 角色属性（生命/能量/招式/动画）均在各自 `.asset` 配置中定义，可用 UFE 配置编辑器（菜单栏 `Window → UFE`）调整。

---

## 9. 玩法建议（如何"畅玩"Demo）

1. **首选进入方式**：主菜单 → **Versus → Player vs CPU**，选择 **Ethan**，场地选 TRAINING ROOM。
2. **热身**：先用 `T/Y/U`（拳）和 `G/H/J`（脚）熟悉普通攻击与移动，按住 `A` 练习防守。
3. **练习必杀**：出招顺序是"**摇杆方向快速输入 → 按攻击键**"。例如发波动拳：`S`(下)→`D`(前)→`T`(轻拳) 一气呵成；按出即会看到角色打出远程气功弹。
4. **放大招**：能量槽满后，输入 `S D S D` + `T`（超必杀·波动）或 + `Y`（超必杀·升龙）。
5. **连招练习**：进入 **Training Mode**，生命自动回血、不会打死，可无限练习 7.3 节连招。
6. **双人对战**：选择 **Versus → Player vs Player**，P1 用键盘左侧，P2 用键盘右侧（方向键+Insert/Home 等），体验完整对战。
7. **随时查看出招表**：战斗中按 `Enter`（P1）或 `Space`（P2）暂停 → 进入 **Special Moves** / **Controls**，游戏内置完整指令表。

---

## 10. 常见问题

| 问题 | 说明 |
|---|---|
| 启动后卡在片头 | 按任意键即可跳过 |
| Online / Bluetooth 按钮灰色 | 旧 UNET 已被 Unity 移除，UFE 自动降级，PC 端联机功能不可用（属预期行为） |
| 想改键位 | 编辑 `ProjectSettings/InputManager.asset` 中对应轴，或用 UFE 配置编辑器（`Window → UFE` → Player Inputs） |
| 想调整难度/回合数/计时 | 打开 `Assets/UFE/Demo/Config.asset`，在 Inspector 中改 `aiOptions.selectedDifficultyLevel`（0~5）、`roundOptions.totalRounds`、`roundOptions.timer` |
| 想直接进入战斗（跳过菜单） | 在 `Config.asset` 的 `debugOptions.startGameImmediately` 设为 `1`，并设 `matchType`（0=训练 / 1=对战），启动即进入加载/战斗 |
| 片头想播视频 | 在 `Config.asset` 的 `gameGUI.introScreen` 改引用 `VideoIntroScreen.prefab`（`StreamingAssets/UFEIntroShort.mp4` 已备好） |
| 报脚本错误 | 确认 `Assets/UFE Addons/Compatibility/UFELegacyAPICompat.cs` 存在；该文件是旧 API 兼容层，**不能删除** |

---

## 11. 兼容层（修复记录摘要）

由于 UFE 2.x 的官方代码使用了 Unity 2018.2 已移除的 API，本项目新增兼容层 `Assets/UFE Addons/Compatibility/UFELegacyAPICompat.cs`，提供：

- `GUITextCompat` / `GUITextureCompat`（旧 OnGUI 文本/纹理）
- `NetworkCompat` / `NetworkViewCompat` / `NetworkPlayerCompat` 等（旧 UNET 网络，方法返回失败，实际降级为单机）
- `MovieTexture` 相关视频播放重写为 `UnityEngine.Video.VideoPlayer`
- 各业务文件通过 `using X = XCompat;` 别名引用兼容类型，**不改动业务逻辑**

涉及文件：`UFE.cs`、`ControlsScript.cs`、`MultiplayerAPI.cs`、`NetworkGameScreen.cs`、`DefaultBattleGUI.cs`、`LanDiscovery.cs`、`NetworkController.cs`、`VideoIntroScreen.cs`、`StoryModeVideoScreen.cs` 等。
