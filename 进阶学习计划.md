# MyprojectDemo（UFE 格斗引擎）进阶精细学习计划

> **适用对象**：非初学者（已掌握 Unity 基础操作 + C# 语法 + 面向对象），不需要从 Unity 扫盲开始。
> **定位**：直接进入 UFE 架构与源码级研究，从"会用"进阶到"懂原理、能改、能扩展"。
> **时间**：不限时长，按模块逐项攻克；每个模块完成后可自查（Checklist），全部过关即为"吃透"。
> **重要更正**：本计划使用**精确源码术语**。此前《学习路径.md》中提到的 `linkMoves`/`chainMoves`/`MoveInput`/`MoveType` 等名称在本版本源码中**不存在**，实际对应关系见第 0 章"术语勘误表"。

---

## 第 0 章 术语勘误表（先读这一章）

> 目的：避免被过时的教程/笔记误导。以下以本项目 `Assets/UFE/Engine/Scripts/Definitions/MoveInfo.cs` 实际代码为准。

| 常见（但错误/过时）说法 | 本项目真实名称（精确位置） |
|---|---|
| `MoveInput` | **`MoveInputs`**（`MoveInfo.cs:786`）——出招输入定义类 |
| `linkMoves` / `chainMoves`（连段链） | **`FrameLink` 数组 + `previousMoves` 数组**（`MoveInfo.cs:989` / `MoveInfo.cs:980`） |
| `MoveType` 枚举 | 实际拆分为多个枚举：`AttackType`、`HitType`、`HitConfirmType`、`FrameSpeed` 等（`MoveInfo.cs`） |
| `GetCharacterMove` / `LoadCharacterMoves` | **`MoveSetScript.cs` 中实际方法**：`LoadCharacterMoves`/`GetMove` 需按源码核对（本版实际为 `MoveSetScript.GetMove` 体系） |
| `aiAsset` 字段 | 角色 AI 指令通过 `AIInstructions` 资产引用（`CharacterInfo.cs` 对应字段按源码核对） |
| 格挡 = "按防御键" | 默认 `BlockType.HoldBack` = **按住后方向**即格挡（`GlobalInfo.cs:40`） |

**核心数据结构一句话总结**（记住这些就是掌握出招系统的钥匙）：
```
一个招式(MoveInfo) = 
  指令定义(MoveInputs.defaultInputs/altInputs: buttonSequence指令序列 + buttonExecution执行按钮)
  + 前置条件(previousMoves / selfConditions / opponentConditions)
  + 连段取消(FrameLink[]: 命中/格挡/破防后允许接入的下一招)
  + 帧数据(startUpFrames / activeFrames / recoveryFrames / totalFrames)
  + 伤害判定(Hit[]: 每段攻击的窗口/伤害/判定类型)
  + 特效与行为(particleEffects / appliedForces / invincibleBodyParts / armorOptions / projectiles)
```

---

## 模块总览

| 模块 | 主题 | 预计投入 | 产出 |
|---|---|---|---|
| M1 | 项目全局扫描（代码地图） | 0.5~1 天 | 一张自己的架构图 |
| M2 | 数据驱动：Definitions 层精读 | 2~3 天 | 术语表 + 数据结构笔记 |
| M3 | 启动流程与屏幕切换（UFE.cs） | 2~3 天 | 启动时序图 + 屏幕切换流程图 |
| M4 | 输入系统全链路 | 2~3 天 | 输入数据流图 |
| M5 | 战斗核心：ControlsScript 状态机 | 3~5 天 | 状态机全图 + 帧流程笔记 |
| M6 | 出招识别与连段（MoveSetScript） | 2~3 天 | 出招匹配算法笔记 |
| M7 | 判定系统：HitBoxes 与伤害 | 2~3 天 | 判定流程图 |
| M8 | 帧同步与时序（FPLibrary/FluxCapacitor） | 2~3 天 | 确定性原理笔记 |
| M9 | UI 事件驱动与导航 | 2~3 天 | UI 事件订阅关系图 |
| M10 | 编辑器工具链（UFE Config 编辑器） | 2~3 天 | 能独立配置一个角色 |
| M11 | AI 系统 | 1~2 天 | AI 决策流程笔记 |
| M12 | 动手实战：改造与扩展 | 5~10 天 | 完成 3 个实战任务 |
| M13 | 进阶专题（网络/动画/兼容层） | 选学 | 专题笔记 |

> 各模块之间**有强依赖顺序**（M3→M4→M5→M6→M7 是主干），建议按顺序推进；M10/M13 可并行。

---

## M1 项目全局扫描（代码地图）

> 目标：不深入细节，先建立"这个引擎有哪些模块、各自干什么"的整体认知。**花半天浏览，不做笔记，只看结构。**

### 1.1 目录扫描任务

1. 浏览 `Assets/UFE/Engine/Scripts/` 的 10 个子目录，对照下表确认每个目录的作用：
   - `Manager/`（4 文件：UFE/ChallengeMode/DelayedAction/InstantiatedGameObject）
   - `Physics/`（5 文件：ControlsScript/MoveSetScript/HitBoxesScript/PhysicsScript/ProjectileMoveScript）
   - `Input/`（7 文件）
   - `UI/Base/`（21 cs）+ `UI/Templates/`（26 cs）
   - `Definitions/`（4 文件：GlobalInfo/CharacterInfo/MoveInfo/StanceInfo）
   - `AI/`（Fuzzy 模糊逻辑 + Random + Simple）
   - `Animation/`（5 文件）
   - `Network/`（27 cs）
   - `FPLibrary/`（12 cs，定点数库）
   - `Mono4Unity/`（94 cs，UFE 移植用的工具类库）
2. 浏览 `Engine/Editor/` 下 14 个编辑器脚本名，理解"每个资产类型对应一个编辑器窗口"。
3. 浏览 `Demo/` 目录，理解"内容资产（角色/场地/UI/声音）与引擎代码分离"。

### 1.2 类继承骨架（用 IDE 画出来）

- 打开 `GlobalInfo.cs`，用大纲视图浏览所有类（GlobalInfo/InputReferences/InputOptions/GameGUI/RoundOptions/DebugOptions/TrainingModeOptions 等）。
- 打开 `MoveInfo.cs`，同样浏览所有类/枚举。
- 重点记住**三棵继承树**：
  ```
  树1（UI 屏幕）: UFEScreen → {MainMenuScreen, VersusModeScreen, ...} → Default*
  树2（角色控制）: ControlsScript（MonoBehaviour）
  树3（输入）   : UFEController → {InputController, RewiredInputController, InputTouchController}
  ```

### 1.3 配置资产盘

- 打开 `Demo/Config.asset`（全局配置）、`Demo/Characters/*/*.asset`（角色+招式）、`Demo/AIInstructions/*.asset`（AI 指令）、`UFE_Config.asset`（全局默认）。
- 逐个在 Inspector 里展开，**不求看懂，只求知道"谁是谁"**。

### M1 Checklist

- [ ] 能不看目录说出 Engine/Scripts 下 10 个子目录各自职责
- [ ] 能说出 3 棵核心继承树
- [ ] 能说出角色/招式/场地/AI/全局配置资产分别在哪、后缀是什么

---

## M2 数据驱动：Definitions 层精读

> 目标：UFE 的核心哲学是"**内容配置化、行为数据化**"。这一层是所有引擎行为的"数据库"，**读懂这一层，后面的代码 80% 是在查表**。

### 2.1 精读 `Definitions/GlobalInfo.cs`

**任务：通读一遍（约 1100+ 行），记笔记。** 重点类：

| 类/枚举 | 行号（约） | 关注点 |
|---|---|---|
| `InputType` | 开头 | 输入类型：HorizontalAxis/VerticalAxis/Button |
| `InputReferences` | 822 | **输入项的完整定义**：`inputType`/`inputButtonName`/`engineRelatedButton`/`joystickAxisName`/cInput 键位 |
| `BlockType` | 40 | 防守方式：None/HoldBack(后拉)/AutoBlock/HoldButton |
| `ParryType` | 172 | 格挡（弹反）方式 |
| `GameGUI` | 中段 | **全部 GUI 预制体引用**（所有屏幕） |
| `RoundOptions` | 中段 | 回合数/计时/重置规则 |
| `InputOptions` | 中段 | `confirmButton`/`cancelButton`/`inputManagerType` |
| `GlobalInfo` | 1108 | 顶层：player1/player2_Inputs、inputOptions、roundOptions、gameGUI 等 |

**思考题**：
1. `InputReferences.engineRelatedButton` 与 `ButtonPress` 枚举什么关系？（答案：输入项映射到引擎动作）
2. 为什么 `confirmButton: 4` 就等于"轻拳键"？（答案：ButtonPress.Button1 = 4）

### 2.2 精读 `Definitions/MoveInfo.cs`

**这是全项目最重要的数据结构文件。** 通读后重点记住：

1. **枚举清单**（务必记牢）：`ButtonPress`(0~20)、`BasicMoveReference`、`InputType`、`PossibleStates`、`SubStates`、`HitType`、`HitStrengh`、`HitConfirmType`、`LinkType`、`CounterMoveType`、`GaugeUsage`、`DamageType`、`AttackType`、`ProjectileType`。
2. **`MoveInputs`**（786 行）：出招输入定义。逐字段理解：
   - `chargeMove`/`_chargeTiming`：蓄力技
   - `allowInputLeniency`/`leniencyBuffer`：输入宽容度（模糊匹配）
   - `allowNegativeEdge`：松键触发
   - `buttonSequence`：**指令序列**（摇杆方向）
   - `buttonExecution`：**执行按钮**
3. **`FrameLink`**（549 行）：连段取消链。字段 `linkType`/`onStrike`/`onBlock`/`onParry`/`activeFramesBegins`/`activeFramesEnds`/`hitStrength`/`hitType`/`linkableMoves` 等——这是"命中确认（Hit Confirm）"系统。
4. **`MoveInfo`**（893 行，ScriptableObject）：招式本体。重点：
   - 帧数据：`totalFrames`/`startUpFrames`/`activeFrames`/`recoveryFrames`（950-952 行）
   - `defaultInputs`/`altInputs`（976-977）
   - `previousMoves`（980）：前置技/派生技
   - `frameLinks`（989）：连段取消
   - `hits`（999）：攻击判定数组
   - `invincibleBodyParts`/`armorOptions`：无敌/霸体
   - `projectiles`（1003）：飞行道具
   - 运行时状态（1005-1016）：`cancelable`/`kill`/`currentFrame`/`hitConfirmOnStrike` 等
5. **`BasicMoveInfo`**（849 行）：基础动作（idle/行走/跳跃/受击）。注意 `BasicMoveReference` 引用。

**练习**：打开 `Ethan/Moves/Stand_DF1.asset`，用编辑器核对：
- `defaultInputs.buttonSequence` 含 `Down(3)`，`buttonExecution` 含 `Button1(4)` → 波动拳 = 下+轻拳
- `previousMoves` 为空（普通必杀技）
- `frameLinks` 里是否配置了可取消的下一招

### 2.3 精读 `Definitions/CharacterInfo.cs` 与 `StanceInfo.cs`

- `CharacterInfo`：`characterName`/`lifePoints`/`maxGaugePoints`/`moves`(MoveInfo[])/`characterPrefab`/`profilePicture`/`aiAsset` 等。**注意 `moves[0].basicMoves` 是基础动作，`moves[1..n]` 是普通技/必杀技**。
- `StanceInfo`：姿态系统（CombatStances，可切换架势）。

### M2 Checklist

- [ ] 能默写 `MoveInputs` 全部字段含义
- [ ] 能解释 `buttonSequence` 与 `buttonExecution` 的区别
- [ ] 能说出 `FrameLink` 在什么条件下允许接入下一招（linkType/onStrike/onBlock/onParry）
- [ ] 能说出 `MoveInfo` 的帧数据 4 字段及含义
- [ ] 能打开一个真实招式资产，指出它的指令序列与执行按钮

---

## M3 启动流程与屏幕切换（UFE.cs）

> 目标：掌握"从点 Play 到主菜单"发生了什么，以及 UFE 如何管理所有界面生命周期。
> 文件：`Assets/UFE/Engine/Scripts/Manager/UFE.cs`（3013 行）。

### 3.1 Region 地图

用 IDE 大纲功能浏览整个文件，对照下表（Region 均为 `#region` 块）：

| Region | 行号区间 | 内容 |
|---|---|---|
| 事件定义 | 51-109 | 所有静态事件（OnGameBegins/OnRoundEnds/OnButton/OnHit...） |
| GUI 定义 | 157-183 | Canvas/AudioSource/config 等 |
| GUI 相关方法 | 628-1307 | 全部 `Start*Screen`（屏幕切换 API） |
| 输入相关 | 1329-1426 | GetButton/GetAxis/输入事件 |
| 角色选择相关 | 1428-1619 | 选人逻辑 |
| 事件触发 | 1660-1745 | 所有 `Fire*` 方法 |
| UFE CORE | 1748-1851 | PauseGame/TimeScale 等 |
| 网络相关 | 1853-1919 | 降级逻辑 |
| MonoBehaviour 方法 | 1922-2249 | **Awake/Update/FixedUpdate/OnGUI** |
| 网络事件 | 2251-2374 | 网络回调 |
| GUI 私有实现 | 2376-3012 | 所有 `_Start*` 私有方法 |

### 3.2 启动流程精读（Awake → 片头 → 主菜单）

**顺序精读**：
1. `Awake()`（1923 起）：
   - 找到 `UFE_Config` → 赋值给 `UFE.config`（**确认：加载的是哪个 asset？——场景里 UFE Manager 拖入的 `Demo/Config.asset`**）
   - 设置 `fixedDeltaTime`、`timeScale`、音效/音乐开关（PlayerPrefs）
   - 预加载角色/场地/特效（`preloadCharacter1/2`、`preloadStage`、`preloadHitEffects`、`warmAllShaders`）
   - 最后（2152 行）：`if (UFE.config.debugOptions.startGameImmediately) {...} else { UFE.StartIntroScreen(0f); }`
2. `StartIntroScreen` → `_StartIntroScreen`（2446）：`HideScreen(当前)` → `ShowScreen(introScreen)`，找不到则 `_StartMainMenuScreen`。
3. `TextureIntroScreen.OnShow()` → 协程等待（任意键 或 3 秒）→ `GoToMainMenu()`。
4. `_StartMainMenuScreen`（2460）：显示主菜单预制体。

**理解屏幕切换套路**（全引擎统一）：
```
StartXxxScreen()                       // 公开：读 fadeDuration
  → StartXxxScreen(float fadeTime)     // 带淡出
      → CameraFade.StartAlphaFade(...) // 半屏淡出
      → DelayLocalAction(..., fade/2)  // 延迟
          → _StartXxxScreen(fade/2)    // 私有：真正切换
              → HideScreen(当前屏)      // OnHide + SetActive(false)
              → ShowScreen(新屏)        // SetActive(true) + OnShow
```

### 3.3 每帧调度链精读

- `Update()`（2178）：`GetPlayer1Controller().DoUpdate()` + `GetPlayer2Controller().DoUpdate()`。
- `FixedUpdate()`（2233）：`fluxCapacitor.DoFixedUpdate()`（**帧同步推进的核心，网络对战时的"时间机器"**）。
- 编辑器模式：`OnGUI()`（2207）提供 F2 存挡 / F3 读挡（状态保存/回放，理解确定性验证的重要工具）。

### 3.4 战斗流程精读

- `_StartGame`（2512）：加载双方角色、场地、BattleGUI，创建 `ControlsScript`。
- `OnRoundEnds` / `OnGameEnds`：回合/整场结算 → 打开对应界面。

### 3.5 事件系统精读

- 事件定义（51-109）：`OnGameBegins`、`OnRoundBegins`、`OnRoundEnds`、`OnGameEnds`、`OnLifePointsChange`、`OnButton`、`OnHit`、`OnBlock`、`OnParry`、`OnTimer`、`OnTimeOver` 等。
- `Fire*` 方法（1660-1745）：如何触发事件。
- **理解事件是 UI 更新的桥梁**（M9 会用到）：BattleGUI 订阅事件 → 更新血条。

### M3 Checklist

- [ ] 能画出"点 Play → 主菜单"的时序图（含方法名）
- [ ] 能写出屏幕切换的通用套路（4 步）
- [ ] 能说出 `Update`/`FixedUpdate` 各自推进什么
- [ ] 能说出 5 个以上 UFE 事件的名称与触发时机
- [ ] 能说出 `startGameImmediately=true` 时启动流程的差异

---

## M4 输入系统全链路

> 目标：完整回答"按 T 键 → 轻拳打出去"的每一环。
> 文件：`ProjectSettings/InputManager.asset`、`Scripts/Input/*`（7 文件）、`ControlsScript.translateInputs`。

### 4.1 数据流（务必画出来）

```
物理键盘
  ↓① InputManager.asset 定义轴
Unity Input 管理器（Input.GetButton("P1Button1")）
  ↓② InputController.ReadInput()
InputEvents（axisRaw + button）
  ↓③ UFE.GetPlayer1Controller() / ControlsScript.DoFixedUpdate(previous, current)
ButtonPress 枚举（engineRelatedButton 映射）
  ↓④ translateInputs()
角色行为（移动/出拳/防守/招式匹配）
```

### 4.2 精读清单

1. **`InputManager.asset`**：对照 `P1KeyboardHorizontal`（a/d）、`P1KeyboardVertical`（s/w）、`P1Button1~6`、`P1Start`(return)、`P2*`（方向键+Insert/Home/PageUp/Delete/End/PageDown/space）。理解轴（Axis，连续值）与按钮（Button，布尔）的区别。
2. **`InputEvents.cs`**：构造重载 `InputEvents(bool)` / `InputEvents(Fix64)`，`button` 与 `axisRaw` 的关系。
3. **`InputController.cs`**：`ReadInput()`（轴用 getAxisRaw、按钮用 getButton）、`InitializeInput()`/`SelectInputType()`（默认 UnityInputManager，cInput 模式设置默认键）。
4. **`UFEController.cs`**：角色控制器的抽象基类（可能含 GetPlayer1/2Controller 的泛型定义）。
5. **`ControlsScript.cs` 的 `translateInputs()`（约 727 起）**：
   - 轴 → 方向：`engineRelatedButton` 动态改为 Forward/Back/Up/Down
   - 斜方向注入（937-1030）：双轴同按 → DownBack/DownForward/UpForward/UpBack
   - 按钮按下/松开（1034-1118）：`buttonDown = ev.button && !p.button`
   - 防守判定、`inputHeldDown`（按住时长，供蓄力/松键用）
6. **`ButtonPress` 枚举**（MoveInfo.cs:12-34）确认数值映射。

### 4.3 实验

- 修改 `InputManager.asset` 中 `P1Button1` 的 `positiveButton`（t → q），Play 验证轻拳变 Q。理解"改配置不改代码"。
- 在 `UFE_Config.asset` 的 `player1_Inputs` 中查看每项 `engineRelatedButton` 与 `InputManager.asset` 的对应。

### M4 Checklist

- [ ] 能默画输入数据流（5 步，含文件）
- [ ] 能说清 `InputEvents` 的 `button` 与 `axisRaw`
- [ ] 能说出斜方向输入怎么生成
- [ ] 能解释"松键触发（negative edge）"在 `inputHeldDown` 上如何实现
- [ ] 能自己改一个键位并说明原理

---

## M5 战斗核心：ControlsScript 状态机

> 目标：吃透"角色每帧做什么、状态怎么流转"。**这是整个引擎的灵魂，花最多时间。**
> 文件：`Assets/UFE/Engine/Scripts/Physics/ControlsScript.cs`（3016 行）。

### 5.1 状态字段（`trackable definitions`，10-73 行）

| 字段 | 类型 | 含义 |
|---|---|---|
| `currentState` | `PossibleStates` | 主状态：Stand/Crouch/NeutralJump/ForwardJump/BackJump/Down |
| `currentSubState` | `SubStates` | 子状态：Resting/MovingForward/MovingBack/Blocking/Stunned |
| `currentMove` / `storedMove` | `MoveInfo` | 当前招式 / 缓冲招式 |
| `currentHit` | `Hit` | 最近命中数据 |
| `stunTime` | `Fix64` | 眩晕剩余 |
| `comboHits`/`comboDamage` | int/Fix64 | 连击统计 |
| `isBlocking`/`blockStunned` | bool | 防守状态 |
| `potentialBlock`/`potentialParry` | bool/Fix64 | 潜在防守/格挡窗口 |
| `isDead` | bool | 死亡 |
| `mirror` | int | 朝向（±1） |
| `inputHeldDown` | Dictionary | 按键按住时长（蓄力/松键） |
| `projectiles` | List | 已发射飞行道具 |

### 5.2 `DoFixedUpdate` 主循环流程（311 行起，务必逐行走查）

顺序为：
1. 懒加载对手引用
2. 训练/挑战模式：生命/能量回血或无限检查（355-378）
3. `UpdateMap()`（382）：更新受击盒位置图
4. `resolveMove()`（386）：**解析待发招式**（缓冲机制）
5. `translateInputs()`（390）：解析输入
6. `validateRotation()`（394）：朝向校验
7. 能量持续消耗 `gaugeDPS`（398-404）
8. 输入查看器 / `UFE.FireButton` 事件（408-419）
9. Root Motion（423-438）
10. 站姿强制 idle / AFK 待机（441-480）
11. 碰撞推挤（486-513）
12. 抖动 shake/shakeCam（518-537）
13. 格挡窗口倒计时（541-544）
14. `ReadMove(currentMove)`（553-556）：**执行当前招式**（核心）
15. `ApplyStun()`（560）：眩晕结算
16. `myPhysicsScript.ApplyForces()`（565）：物理力
17. 开局 intro / 换局逻辑（569-577）

### 5.3 关键方法深读

| 方法 | 位置（约） | 内容 |
|---|---|---|
| `ReadMove` | — | 招式执行：按 currentFrame 推进 Startup/Active/Recovery |
| `GetHit` | — | 受击反应：硬直/击退/倒地判定 |
| `TakeDamage`/`DamageMe` | 2868 | 伤害结算：防守减伤/连击衰减/能量回复 |
| `CheckHitBoxes` | — | 攻击判定框检测命中 |
| `Blocking`/`CheckBlocking` | — | 防守判定（HoldBack/按钮防守） |
| `FireButton` 相关 | 408-419 | 触发 UFE 事件 |
| `ApplyStun` | 560 | 眩晕 |

### 5.4 帧数据运行态（理解 ReadMove 的关键）

`CurrentFrameData` 枚举：`StartupFrames` / `ActiveFrames` / `RecoveryFrames`，由 `currentFrame` 与 `MoveInfo.startUpFrames/activeFrames/recoveryFrames` 比较得出。**这是"前摇/判定/后摇"的运行时实现。**

### 5.5 训练模式特殊逻辑

- 生命：`p1Life`/`p2Life` = `Refill`（回血）/`Infinite`（无限）/`Normal`
- 能量：`p1Gauge`/`p2Gauge` 同理（Config 中当前 `p1Gauge: 2` = Infinite）
- 实现位置：`DoFixedUpdate` 步骤 2 + `DamageMe`/`RefillGauge`/`RefillLife`

### 5.6 练习（调试实战）

1. `Config.asset → debugOptions.debugMode = true`，进训练模式，屏幕显示 FPS/生命/当前状态/当前招式。
2. 发一个波动拳，观察 `currentMove`/`currentFrameData`/`currentState` 变化。
3. 打开 `ControlsScript.cs`，对照调试信息找到对应代码分支。

### M5 Checklist

- [ ] 能默写 `DoFixedUpdate` 17 步流程
- [ ] 能画出 PossibleStates + SubStates 的状态机
- [ ] 能解释 Startup/Active/Recovery 如何由 currentFrame 判定
- [ ] 能说清伤害结算路径（攻击框命中 → TakeDamage → 硬直）
- [ ] 能说明训练模式回血/无限的代码实现位置

---

## M6 出招识别与连段（MoveSetScript）

> 目标：理解"输入的按钮序列怎么变成出招"。这是格斗引擎最有含金量的部分。
> 文件：`Scripts/Physics/MoveSetScript.cs`、`Definitions/MoveInfo.cs`。

### 6.1 数据结构回顾（M2 已精读）

- `MoveInputs.buttonSequence`：方向序列（如 Down→Forward）
- `MoveInputs.buttonExecution`：执行按钮（如 Button1）
- `previousMoves`：派生技前置
- `FrameLink[]`：命中后取消链

### 6.2 精读 MoveSetScript 核心方法

| 方法 | 职责 |
|---|---|
| 初始化 | 加载角色所有招式（basicMoves + moves）建立查询索引 |
| `GetMove` | **输入序列匹配**：把当前缓冲输入与所有招式比对，返回命中的 MoveInfo |
| 派生技逻辑 | 检查 `previousMoves` 是否包含当前招式 → 允许后续技 |
| 连段逻辑 | 检查 `frameLinks`（命中/格挡/破防）→ 允许取消到 linkableMoves |

### 6.3 出招匹配算法精读（重点）

1. **指令序列如何累积**：`ControlsScript` 里 `lastButtonPresses`/`ButtonSequenceRecord` 记录输入历史（通常 30 帧窗口，`executionBufferTime`）。
2. **匹配顺序**：遍历角色招式表 → 每个招式比对指令序列 → 全部匹配则候选 → 再按优先级选择。
3. **宽容机制**：`allowInputLeniency` + `leniencyBuffer`（模糊匹配多余输入）；`allowNegativeEdge`（松键也算一次输入）。
4. **缓冲**：`storedMove` 与 `resolveMove()`——输入提前量。

### 6.4 练习（用资产验证）

打开 `Ethan/Moves/Stand_DF1DF1.asset`（超必杀波动），核对：
- `previousMoves` 引用了 `Stand_DF1`（必须先发普通波动拳）
- `defaultInputs.buttonSequence` = Down(3)
- 结合 `M5` 的调试模式，实际体验"先普通波动 → 再超必杀"的派生流程

### M6 Checklist

- [ ] 能说出输入序列匹配的完整流程（从按键到 GetMove 返回）
- [ ] 能解释 `previousMoves` 如何实现派生技
- [ ] 能解释 `FrameLink` 如何实现连段取消（命中确认）
- [ ] 能说出宽容机制 3 个参数的作用
- [ ] 能用资产文件举例说明派生技配置

---

## M7 判定系统：HitBoxes 与伤害

> 目标：理解"攻击框 vs 受击框"的碰撞判定与伤害计算。
> 文件：`Physics/HitBoxesScript.cs`、`Physics/ProjectileMoveScript.cs`、`Physics/PhysicsScript.cs`。

### 7.1 HitBox 数据结构

- `HitBox`：`bodyPart`（身体部位）、`position`（Transform，判定盒位置）、`enabled`（开关）、`defaultVisibility`。
- `HitBoxesScript`：角色身上所有判定盒的管理器。与 `animMap`（动画帧 → HitBox 位置映射）联动——**每帧把判定盒移动到动画指定位置**。

### 7.2 判定流程精读

```
攻击方：招式 active 帧内，攻击框(enabled=true) 在移动
   ↓ 碰撞检测（CheckHitBoxes）
防守方：受击框 与 攻击框 相交
   ↓ 判定优先级
防守（Blocking）？→ 格挡
格挡窗口（potentialParry）？→ 弹反（Parry）
否 → 命中：HitType/HitStrengh 决定受击动画 → TakeDamage
```

### 7.3 伤害计算精读

- `DamageType`（Percentage/Points）：伤害按百分比还是点数。
- 连击衰减：`comboHits` 影响伤害。
- 能量回复：`gaugeGainOnHit/onBlock/onMiss`（MoveInfo 字段）。
- 防守减伤/破防（Crumple）。

### 7.4 飞行道具（ProjectileMoveScript）

- `Projectile` 定义在 MoveInfo（`projectiles` 数组）：发射帧/速度/判定框。
- `ProjectileMoveScript`：飞行道具实例的移动、命中、消失。

### 7.5 练习

1. 打开角色选择界面（`displayMode = CharacterGameObject` 时）或训练模式，观察/调试判定盒（`hitBox.position` 物体）。
2. 修改一个招式资产的 `hits[0].damage`，进游戏验证伤害变化。

### M7 Checklist

- [ ] 能画出判定流程图（攻击框→受击框→防守/命中分支）
- [ ] 能说出 HitBox 与 animMap 的联动方式
- [ ] 能说出伤害计算的 3 个影响因素（类型/连击衰减/防守）
- [ ] 能说清弹反（Parry）触发条件

---

## M8 帧同步与时序（FPLibrary / FluxCapacitor）

> 目标：理解 UFE 为什么用定点数，以及"确定性"如何保证。
> 文件：`Scripts/FPLibrary/*`、`UFE.cs` 中 `fluxCapacitor`、`DelayedAction`/`DelaySynchronizedAction`。

### 8.1 定点数（FPLibrary）

- 通读 `FPLibrary` 12 个文件，理解：
  - `Fix64`：用 `long` 存定点数（如精度 1/65536），避免 float 的机器差异。
  - `FPMath`：对应 `Mathf` 的函数族（Sin/Abs/Sign/Lerp）。
  - `FPVector`/`FPLibrary`：定点向量/工具。
- **为什么格斗游戏必须用定点数**：联网对战时，所有机器必须对"相同输入"产生"完全相同的结果"。float 在不同 CPU/编译优化下可能差一位小数，导致不同步；定点数从数学上消除这个差异。

### 8.2 FluxCapacitor（帧同步核心）

- 在 `UFE.cs` 中搜索 `fluxCapacitor`：
  - `FixedUpdate` 中 `fluxCapacitor.DoFixedUpdate()` 推进"帧时钟"。
  - 存挡/读挡（F2/F3）就是 FluxCapacitor 的状态序列化——验证确定性。
  - `DelaySynchronizedAction`（264-355 region）：**跨客户端同步延迟动作**（如回血、特效）。
- 理解 **固定帧率**：`fixedDeltaTime`（默认 1/60s），所有逻辑在 `DoFixedUpdate`，保证确定性。

### 8.3 输入缓冲与手感

- `executionBufferTime: 30`（帧）：出招缓冲窗口。
- `plinkingDelay: 2`：双键连按窗口（Plink 技巧）。
- `gameSpeed`：全局速度。

### M8 Checklist

- [ ] 能解释 Fix64 的实现思路（long + 精度）
- [ ] 能说出"确定性"对格斗游戏的意义
- [ ] 能说出 FluxCapacitor 在 FixedUpdate 中的角色
- [ ] 能说出 executionBufferTime/plinkingDelay 对"手感"的影响

---

## M9 UI 事件驱动与导航

> 目标：理解 UI 怎么从"游戏事件"获取数据，以及菜单导航机制。
> 文件：`UI/Base/*`、`UI/Templates/*`、`UFEScreenExtensions.cs`。

### 9.1 UI 三层架构

```
UFEScreen（基类：DoFixedUpdate/OnShow/OnHide/SelectOption）
  → Base 屏幕类（业务语义：MainMenuScreen.GoToStoryMode 等）
    → Templates 实现（Default*：字段 + 逻辑）
      → 预制体（Demo/GUI/UI_Prefabs/*.prefab）
        → 配置引用（Config.asset → gameGUI.*）
```

### 9.2 Base 层精读（UI/Base/ 21 个文件）

| 类 | 关键内容 |
|---|---|
| `UFEScreen` | 基类：4 个虚方法 + 公共字段（firstSelectableGameObject/hasFadeIn/wrapInput） |
| `MainMenuScreen` | 9 个跳转方法（GoToVersusMode/GoToStoryMode/GoToTrainingMode...） |
| `VersusModeScreen` | SelectPlayerVersusPlayer/Cpu、SelectCpuVersusCpu |
| `CharacterSelectionScreen` | `GetHoverIndex`/`SetHoverIndex`/`OnCharacterSelectionAllowed`/`TrySelectCharacter`/`TryDeselectCharacter`（联网走 FluxCapacitor 帧延迟同步） |
| `StageSelectionScreen` | `OnStageSelectionAllowed`/`TrySelectStage` |
| `BattleGUI` | 订阅全部 UFE 事件；空虚方法 `OnGameBegin/OnRoundBegin/OnLifePointsChange/OnHit/OnBlock/OnTimer...` |
| `PauseScreen` | `ResumeGame`/`GoToMainMenu` |
| `LoadingBattleScreen` | `StartBattle()` → `UFE.StartGame()` |
| `OptionsScreen` | 音量/AI 难度/调试模式设置虚方法 |
| `VersusModeAfterBattleScreen` | 内部 `enum Option{RepeatBattle, CharacterSelection, StageSelection, MainMenu}` |

### 9.3 Templates 层精读

- 每个 `Default*` 都是对应 Base 的**默认实现**，关键：
  - `DefaultMainMenuScreen`：`DefaultNavigationSystem`（通用导航）+ 按钮字段。
  - `DefaultBattleGUI`：血条渐变（`targetLife`）、回合提示、计时、输入查看器。**逐字段核对** `PlayerGUI`（name/portrait/lifeBar/gaugeMeter/wonRoundsImages/alert）。
  - `DefaultCharacterSelectionScreen`：双人独立导航（PvP 两套 SpecialNavigationSystem）、大立绘/3D 模型展示、HUD 游标。
  - `DefaultPauseScreen`：`screens[]` 数组 + `GoToScreen(index)` 子界面切换 + 确认对话框。

### 9.4 导航系统精读（UFEScreenExtensions.cs）

- `DefaultNavigationSystem`：移动光标 + 确认 + 取消（confirmButton/cancelButton）。
- `SpecialNavigationSystem`：角色选择时双人独立操作。
- `HighlightOption`/`MoveCursor`/`FindSelectable`：基于 `Selectable.allSelectables` 的定位。
- **取消动作**：`cancelAction` 参数（如 `GoToMainMenu`）。

### 9.5 事件驱动练习

- 在 `DefaultBattleGUI.cs` 里搜索 `OnLifePointsChange`/`OnTimer` 订阅与回调，画出"游戏数据 → 血条/计时"的更新链路。
- 思考：为什么 UI 用事件而非每帧轮询？（解耦 + 只在变化时更新）

### M9 Checklist

- [ ] 能画出 UI 四层架构（基类/模板/预制体/配置）
- [ ] 能说出 BattleGUI 订阅的 5+ 个事件
- [ ] 能解释 DefaultNavigationSystem 的确认/取消键来源
- [ ] 能说出角色选择如何支持双人独立操作
- [ ] 能画出"血条更新"的事件链路

---

## M10 编辑器工具链

> 目标：掌握"怎么用 UFE 的配置编辑器改角色/招式"，并理解其背后的 EditorWindow 原理。
> 文件：`Engine/Editor/*.cs`（14 个）。

### 10.1 工具地图

| 编辑器脚本 | 编辑对象 | 打开方式 |
|---|---|---|
| `GlobalEditorWindow` | 全局配置（GUI/回合/输入/难度） | Unity 菜单 → UFE → Global Editor |
| `CharacterEditorWindow` | 角色（基础属性/招式列表/AI） | 同上 → Character Editor |
| `MoveEditorWindow` | 单个招式（帧数据/输入/判定/特效） | 同上 → Move Editor |
| `StanceEditor` | 姿态 | 同上 |
| `AIEditorWindow`/`SimpleAIBehaviourEditorWindow` | AI 指令 | 同上 |
| `UFEInputSetup` | 输入轴一键配置 | 菜单 |

### 10.2 编辑器原理精读

- 以 `GlobalEditorWindow.cs` 为例：
  - `[MenuItem]` 菜单注册。
  - `EditorWindow.GetWindow<T>()` 创建窗口。
  - `OnGUI()` 绘制界面。
  - 如何读写 `ScriptableObject`（`AssetDatabase`/`ScriptableObjectUtility`）。
- 理解"资产编辑器"模式：**数据在 .asset，界面在 EditorWindow，运行时代码只读数据**。

### 10.3 实战：用编辑器配置一个招式

1. 打开 Move Editor，选择 Ethan 的某个招式。
2. 修改 `startUpFrames`/`activeFrames`/`recoveryFrames`，保存。
3. 进游戏对比手感（前摇长=难用、后摇长=易被惩罚）。
4. 修改 `buttonSequence`，把招式改成新指令，进游戏验证。

### M10 Checklist

- [ ] 能独立打开 5 类编辑器窗口
- [ ] 能说出 EditorWindow 的关键生命周期方法（OnGUI 等）
- [ ] 能改一个招式的帧数据并验证效果
- [ ] 能新建一个招式资产

---

## M11 AI 系统

> 目标：理解电脑对手如何决策。
> 文件：`Config.asset → aiOptions`、`Scripts/AI/`（Fuzzy/Random/Simple）、`Demo/AIInstructions/`。

### 11.1 难度参数精读

打开 `Config.asset → aiOptions`：
- `engine: 1`：Fuzzy 引擎。
- `difficultySettings[]`：6 档难度，对比 `timeBetweenDecisions`（决策间隔）、`timeBetweenActions`、`comboEfficiency`（连招效率）。
- `selectedDifficultyLevel: 2`。

### 11.2 AI 决策流程

- Fuzzy 模糊逻辑：对每个可选动作打分 → 选分最高者。
- `AIAsset.cs`：AI 指令资产结构（每个角色/招式配一个）。
- `SimpleAIBehaviourEditorWindow`：可视化编辑 AI 行为。

### 11.3 实验

- 难度 0 vs 5 各打一场 PvCPU，体会差距并解释到参数上。

### M11 Checklist

- [ ] 能解释难度参数如何影响强弱
- [ ] 能说出 Fuzzy AI 的决策思路（评分制）
- [ ] 能找到某角色对应的 AIInstructions 资产

---

## M12 动手实战：改造与扩展

> 目标：把知识变成能力。完成以下任务（建议用 git 分支管理，每个任务独立提交）。

### 任务 1：新增角色（3~5 天）
1. 复制 `Demo/Characters/Ethan/` 整个目录 → 重命名 `MyFighter/`。
2. 修改 `MyFighter.asset`：`characterName`/`lifePoints`/`maxGaugePoints`。
3. 用 Move Editor 复制/修改一个必杀技（改伤害、指令）。
4. 将新角色加入 `Config.asset → characters` 列表。
5. 在 `CharacterSelectionScreen.prefab` 添加角色槽位（复用现有角色按钮结构）。
6. 进游戏验证：选人界面出现、战斗可用、出招正常。

### 任务 2：自定义一个招式（2~3 天）
1. 新建 Move asset（通过 Move Editor 的 Create）。
2. 配置帧数据（参考 Ethan 现有招式）。
3. 配置 `buttonSequence`/`buttonExecution`。
4. 配置 1 个 Hit（伤害/判定窗口）。
5. 挂到角色 moves 列表，进游戏验证。

### 任务 3：新增一个屏幕（3~5 天）
1. 新建 `MyScreen.cs : UFEScreen`（自定义 `DoFixedUpdate`/`OnShow`）。
2. 用 UI 搭建一个简单界面（复制 MainMenuScreen.prefab 改造）。
3. 把新屏幕挂到脚本，在 `Config.asset → gameGUI` 里加引用。
4. 从主菜单加一个按钮跳转过去（改 MainMenuScreen.prefab 的按钮 onClick）。

### 任务 4（进阶）：修改连段系统
- 研究 `FrameLink`，给 Ethan 增加一个新连段（如"重拳命中后取消升龙"），验证 `onStrike` 条件下可取消。

### 任务 5（挑战）：改输入缓冲
- 修改 `executionBufferTime`（30→60），体验手感差异，理解缓冲对"连招成功率"的影响。

### M12 Checklist

- [ ] 完成至少 3 个任务
- [ ] 每个任务都能在游戏中看到效果
- [ ] 能解释每个改动涉及的数据结构与代码位置

---

## M13 进阶专题（选学）

### 13.1 网络架构（当前不可用但值得学）
- `Scripts/Network/`（27 cs）：旧 UNET 多层网络（服务器-客户端、帧同步）。
- `UFE.cs` 反射加载：`SearchClass("UnetHighLevelMultiplayerAPI")` → 不存在则降级 `NullMultiplayerAPI`。
- 兼容层 `NetworkCompat`：理解"API 演化下的优雅降级"。

### 13.2 动画系统
- `MecanimControl.cs` vs `LegacyControl.cs`：两套动画驱动，如何把"游戏状态"转成"动画播放"。
- `animMap`（SerializedAnimationMap/AnimationMap）：**逐帧判定盒映射**。
- `MC_Controller.controller`：Animator 状态机。

### 13.3 兼容层深读
- `UFE Addons/Compatibility/UFELegacyAPICompat.cs`：GUIText/Network/MovieTexture 的 Compat 实现。
- `using GUIText = GUITextCompat;` 别名替换的**适配器模式**应用。

### 13.4 Mono4Unity / 第三方库
- `Mono4Unity/`（94 cs）：UFE 移植用工具库，浏览了解。

### 13.5 性能与确定性
- `warmAllShaders`、`preloadingTime`：启动预加载。
- 固定帧 + 定点数 + FluxCapacitor 三件套对确定性的意义。

---

## 附录 A：全项目核心文件速查表（进阶版）

| 学习主题 | 文件（精确路径） |
|---|---|
| 全局入口/启动 | `Assets/UFE/Engine/Scripts/Manager/UFE.cs` |
| 帧同步时钟 | 同上 → `fluxCapacitor`、`FixedUpdate` |
| 全局数据结构 | `Scripts/Definitions/GlobalInfo.cs` |
| 招式数据结构 | `Scripts/Definitions/MoveInfo.cs`（MoveInputs:786 / FrameLink:549 / MoveInfo:893） |
| 角色数据结构 | `Scripts/Definitions/CharacterInfo.cs` |
| 姿态系统 | `Scripts/Definitions/StanceInfo.cs` |
| 输入事件 | `Scripts/Input/InputEvents.cs` |
| 输入实现 | `Scripts/Input/InputController.cs` / `UFEController.cs` |
| 角色战斗核心 | `Scripts/Physics/ControlsScript.cs`（DoFixedUpdate:311 / translateInputs:727） |
| 出招识别 | `Scripts/Physics/MoveSetScript.cs` |
| 判定盒 | `Scripts/Physics/HitBoxesScript.cs` |
| 物理移动 | `Scripts/Physics/PhysicsScript.cs` |
| 飞行道具 | `Scripts/Physics/ProjectileMoveScript.cs` |
| UI 基类 | `Scripts/UI/Base/UFEScreen.cs` |
| UI 导航 | `Scripts/UI/Templates/UFEScreenExtensions.cs` |
| 战斗 HUD | `Scripts/UI/Templates/DefaultBattleGUI.cs` |
| 选人界面 | `Scripts/UI/Templates/DefaultCharacterSelectionScreen.cs` |
| 暂停界面 | `Scripts/UI/Templates/DefaultPauseScreen.cs` |
| 定点数库 | `Scripts/FPLibrary/`（Fix64/FPMath） |
| AI | `Config.asset → aiOptions` + `Scripts/AI/Fuzzy/` + `Demo/AIInstructions/` |
| 编辑器工具 | `Engine/Editor/`（GlobalEditorWindow/CharacterEditorWindow/MoveEditorWindow） |
| 动画控制 | `Scripts/Animation/MecanimControl.cs` / `LegacyControl.cs` |
| 兼容层 | `UFE Addons/Compatibility/UFELegacyAPICompat.cs` |
| 全局配置 | `Assets/UFE/Demo/Config.asset` |
| 输入轴定义 | `ProjectSettings/InputManager.asset` |

## 附录 B：学习路线图（不限时间，按序推进）

```
M1 全局扫描 ──► M2 Definitions ──► M3 UFE.cs ──► M4 输入
                                         │
                                         ▼
M9 UI ◄── M8 帧同步 ◄── M7 判定 ◄── M6 出招识别 ◄── M5 ControlsScript
   │
   ▼
M10 编辑器 ──► M11 AI ──► M12 实战 ──► M13 进阶专题
```

> 主线（M2→M3→M4→M5→M6→M7→M8）是理解引擎的核心链路，务必按序。
> M9/M10/M11 依赖主线但可灵活穿插；M12 建议在主线完成后集中做；M13 完全按兴趣。

## 附录 C：推荐自建笔记体系

```
00_数据结构.md      # M2：所有枚举/类的字段含义
01_启动时序.md      # M3：点Play到主菜单的时序图
02_输入链路.md      # M4：数据流图
03_状态机.md        # M5：状态流转图 + DoFixedUpdate 流程
04_出招识别.md      # M6：匹配算法
05_判定与伤害.md    # M7：流程图
06_确定性原理.md    # M8：定点数 + 帧同步
07_UI事件.md        # M9：事件订阅关系
08_编辑器.md        # M10
09_AI.md            # M11
10_实战记录.md      # M12：每个任务的改动与结果
```

---

> 一句话总结学习心法：**先建"数据字典"（M2），再追"时序主线"（M3-M5），然后验证"判定细节"（M6-M7），最后用"实战"检验真懂（M12）**。祝顺利吃透这个引擎。
