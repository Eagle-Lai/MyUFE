# 2026-08-24

## 重排版 UFE类图详细说明文档.docx
- 用户反馈旧版排版混乱不清晰。分析数据：4550 成员中字段占 2247（49%），旧版每个成员一个段落且缩进雷同 → 看起来像流水账。
- 重写 `g:\MyUFE\.codebuddy\generate_docx.js`，全新排版方案（表格化）：
  - 每个类型一个 3 列表格：类别 / 成员 / 说明；类别列着色标签（方法蓝 1F4E79 / 属性绿 2E7D32 / 字段棕 8D6E63 / 事件紫 6A1B9A / 枚举成员青 00695C）。
  - 成员列：第一行成员名加粗、第二行完整签名（Consolas 8pt 灰）；说明列无注释显示"（无说明）"。
  - 表格表头行深蓝底白字 + tableHeader 重复、每个表格显式 tblBorders 边框。
  - 层级：H1 部分（16 个含指南/目录/附录）→ H2 文件（213）→ H3 类型（439，◆类型名 格式）。
  - 页脚页码"第 N 页 / 共 M 页"；TOC 域 `TOC \h \o "1-3"`（dirty 打开自动更新）。
- 输出路径改为 `g:\MyUFE\Assets\___Doc\UFE类图详细说明文档.docx`（258 KB，345 表格/4379 行已验证）。
- 数据源路径同步改为 `Assets\___Doc\members_analysis.json`。
- 验证方式（docx 结构检查）：node + `execFileSync('tar',['-xOf',file,entry],{maxBuffer:512MB})` 直接 spawn tar（**不能走 cmd**，中文路径经 cmd 会乱码；也不能用 `node -e` 内联含中文，同样乱码，必须写成 .js 文件 UTF-8 执行）。
- 生成脚本 `.codebuddy\generate_docx.js`，重跑命令：`node g:\MyUFE\.codebuddy\generate_docx.js`。

## TestDemo 战斗功能开发指南.docx
- 用户新建 `Assets\TestDemo\`（test.unity 场景 + Ethan/Robot_Kyle 两个预制体），要从零逐步实现简单战斗（摄像机控制/移动/攻击/技能），自己敲代码、由简到繁。
- 场景分析：主相机 FOV60 透视、TrainingRoom 地面、Ethan(-7.75,0,0)、Robot_Kyle(4.92,0,0,Y-90°)。
- 关键发现：两预制体 Animator 共用 UFE 的 **MC_Controller.controller**（`Assets\UFE\Engine\Resources\`，仅 Mirror 参数、状态机为空）→ 无现成动画，前期用位移/特效模拟动作，真动画留扩展阶段。
- 产出 `Assets\___Doc\TestDemo战斗功能开发指南.docx`（25 KB）：11 步渐进式开发手册（CameraFollow→PlayerController→PlayerAttack→Health→AttackHit→Skill/Projectile→EnemyAI→受击反馈→UI 血条/技能按钮→GameManager），每步含 Unity 操作/脚本设计/代码骨架(灰底代码块)/验证/学习点，另含扩展路线 6 方向与常见坑排查表。
- 生成脚本 `.codebuddy\generate_battle_guide.js`，重跑：`node g:\MyUFE\.codebuddy\generate_battle_guide.js`。
- 文档结构验证全通过（2 表/7 H1/11 H2/代码块/TOC/页脚）。

## TestDemo 双目录双命名空间约定 + AIScripts 参考脚本
- 用户要求：AI 写的参考脚本放 `Assets\TestDemo\AIScripts\`（namespace AIScripts），用户照着敲的放 `Assets\TestDemo\MyScripts\`（namespace MyScripts）。
- 已生成 11 个完整参考脚本到 AIScripts（CameraFollow/PlayerController/PlayerAttack/Health/AttackHit/Skill/SkillController/Projectile/EnemyAI/HealthBarUI/GameManager，均 namespace AIScripts，含第 6 步判定球生成与第 9 步受击反馈整合）。
- 文档更新：阅读指南+1.3 工程约定改为双目录双命名空间说明，每步"脚本："行拆为"参考实现(AIScripts)/自己敲入(MyScripts)"，第二章架构图加目录说明。
- 新文档内容验证通过（AIScripts/MyScripts 各 27 处）。用户关闭 Word 后已用 copy_doc.js 成功替换正式路径 `Assets\___Doc\TestDemo战斗功能开发指南.docx`（25.8KB），临时文件与 copy 脚本均已清理。

## 学习节奏调整：分步供给（用户明确要求）
- 用户要求"一步步来，不要一次性把所有脚本生成出来"：**AIScripts 目录每步只保留当前步骤的参考脚本**，其余 10 个脚本（PlayerController/PlayerAttack/Health/AttackHit/Skill/SkillController/Projectile/EnemyAI/HealthBarUI/GameManager，含 .meta）已暂存到 `g:\MyUFE\.codebuddy\testdemo_reference\AIScripts\`。当前 AIScripts 仅剩 CameraFollow.cs（第 2 步）。每完成一步验证通过后，用户向 AI 索取下一步参考脚本。
- 文档（generate_battle_guide.js）每步新增"脚本关系"小节（step() 函数支持 opts.relation，渲染在目标之后/Unity 操作之前），说明该步涉及哪些脚本、与已完成脚本的依赖协作。阅读指南新增"分步供给"说明；第二章架构图前加"最终全貌图仅供建立整体认知，实现时按第三章逐块进行"说明。文档已重新生成（27 KB，11 个脚本关系小节验证通过）。
