# 2026-08-22 工作日志

## 生成 ClassDiagram1.cd 类图阅读文档（.docx）
- 任务：分析工程根目录 `g:\MyUFE\ClassDiagram1.cd`（Visual Studio 类设计器类图文件，约 3100 行 XML）并生成 .docx 阅读文档。
- 成果：`g:\MyUFE\ClassDiagram1_阅读文档.docx`（25459 字节，已验证内容完整）。
- 分析方法：写 Node 临时脚本解析 .cd XML，提取全部类型（439 个：Class 295 / Enum 93 / Struct 26 / Delegate 13 / Interface 12），按源文件（213 个）和模块分组统计；结合 UFE 源码阅读核心类职责。
- 文档结构：封面+目录(TOC)+1文档说明+2类图文件概览(格式解析)+3总体统计+4模块详细分析(4.1 Definitions 138 / 4.2 Manager 4 / 4.3 Physics 11 / 4.4 Input 7 / 4.5 AI 32 / 4.6 Animation 9 / 4.7 Camera 2 / 4.8 Network 34 / 4.9 FPLibrary 9 / 4.10 UI 48 / 4.11 Mono4Unity 129 / 4.12 Compatibility 14)+5关键类型速查表+6阅读建议+附录A/B。
- 技术要点：用 docx-js 全局库（npm install -g docx，NODE_PATH=C:\Users\lzy\AppData\Roaming\npm\node_modules）生成；中文字体 eastAsia 'Microsoft YaHei' + ascii 'Arial'；A4 页边距 1 英寸；表头 DCE9F5、隔行 F5F9FD 底纹。
- 踩坑记录：
  1) 本次会话中 write_to_file 工具反复报 "missing required argument filePath"（连最小文件也失败），但 replace_in_file 正常 → 改用 replace_in_file 在已有文件末尾追加内容（old_str=文件结尾锚点）的方式构建内容模块，规避了问题。
  2) PowerShell 命令行传递中文文件名/中文关键词会 GBK/UTF-8 错乱（"阅读文档" 变成 "闃呰鏂囨。"）→ 用通配符 ClassDiagram1_*.docx 或 Node 脚本（Node 直接读 UTF-8）验证。
  3) docx 包必须设 NODE_PATH 才能被项目内脚本 require。
- 临时文件已全部清理。注意 `___temp` 目录是项目自带的 Assets 副本，勿删。

## 生成《UFE类图详细说明文档.docx》（成员级全量解析）
- 任务：按用户要求，根据类设计图生成"成员级"详细说明文档——说明每个代码文件中的方法/属性/枚举的具体作用。
- 成果：`g:\MyUFE\UFE类图详细说明文档.docx`（165KB，5434 段落 / 16 个 H1 / 213 个 H2 / 439 个 H3，已验证结构完整）。
- 关键发现：ClassDiagram1/2/3.cd **三个类图内容完全一致**（各 439 个类型），覆盖 `Assets/UFE/Engine/Scripts` 与 `Assets/UFE Addons` 共 **213 个源码文件、439 个类型、4550 个成员**。这是 UFE（Universal Fighting Engine）格斗引擎框架。
- 分析流程：① Python 脚本解析 .cd XML 提取 类型→源文件 映射；② Python 静态分析 213 个 .cs 文件，按行提取类型声明/方法/属性/字段/事件/枚举成员 + 紧邻的 `///` XML 注释（方法签名跨行时拼接至右括号）；③ Node + docx-js 生成文档。
- 文档结构：封面 → 阅读指南 → 自动目录(TOC, updateFields) → 13 个功能模块章节（核心/定义数据/角色物理/AI/动画/相机/输入/网络/定点数学/UI/Mono4Unity/旧版兼容/工具）→ 附录统计。每文件=H2（路径+类型清单），每类型=H3（类型说明+逐成员【方法】签名+说明）；Mono4Unity(.NET 兼容层)用紧凑列表模式控制篇幅。
- 成员说明文字直接取自源码中文 XML 注释（该 UFE 工程注释已中文化，质量高）；无注释成员仅列签名。
- 可复用文件保留在 `.codebuddy/`：`generate_docx.js`（重新生成/定制文档）+ `members_analysis.json`（213文件×4550成员的结构化数据源）。重新生成：`node g:\MyUFE\.codebuddy\generate_docx.js`。
- 环境要点：系统 Python 是 2.7（脚本需 py2 兼容写法，json 用 ascii 编码写文件）；docx-js 用全局包（`npm root -g` 动态 require）；cmd 传中文参数会乱码，验证脚本写成 .py 文件再执行。

## 文档归档至 Assets/___Doc + 写入永久记忆
- 按用户要求，将 `UFE类图详细说明文档.docx`、`ClassDiagram1_阅读文档.docx`、`members_analysis.json` 移动到 `g:\MyUFE\Assets\___Doc\`（目录已创建，Unity 导入 docx/json 为普通资源无害）。
- `generate_docx.js` 保留在 `.codebuddy/`（.js 不可放 Assets，Unity 会当 UnityScript 编译）。归档位置与重新生成方法已写入 MEMORY.md。
