# 2026-08-27

## TestDemo 学习项目进度
- 第 7 步（技能系统）用户验证通过后进入第 8 步。
- 第 8 步（简单敌人 AI）已讲解完成，`EnemyAI.cs` 已移入 `Assets\TestDemo\AIScripts\`（现 9 个脚本：AttackHit/CameraFollow/EnemyAI/Health/PlayerAttack/PlayerController/Projectile/Skill/SkillController）。核心：最简状态机 Idle/Chase/Attack（enum+switch），Chase 用 cc.Move 追击，Attack 用间隔计时 TakeDamage，超范围回退。
- 暂存目录 `.codebuddy\testdemo_reference\AIScripts\` 剩余 2 个脚本：GameManager.cs、HealthBarUI.cs。
- 下一步（第 9 步）：受击反馈（Health.TakeDamage 加方向参数、闪红/击退/命中特效），涉及修改 Health.cs + AttackHit.cs + Projectile.cs 三处联动。

## 第 9 步（受击反馈）已讲解
- 无新独立脚本：参考实现已整合在 AIScripts 的 Health.cs（TakeDamage(int,Vector3) 重载保留旧签名 + FlashRed 协程，material 用实例化材质而非 sharedMaterial）、AttackHit.cs/Projectile.cs 已用新签名 h.TakeDamage(damage, dir)。暂存目录剩余 GameManager.cs、HealthBarUI.cs 对应第 10/11 步。
- 下一步（第 10 步）：UI 血条 + 技能按钮（HealthBarUI.cs）。
- 备注：用户另贴过 G:\MainExUIVertical_PC 工程的 HybridCLR StripAOTDllCommand UnityLinker.exe 找不到的构建报错（Win32Exception），与 TestDemo 无关，用户已取消未处理。
- 用户检查结果：第 9 步已完整实现（Health 带方向重载+FlashRed 用 material 实例化材质、AttackHit/Projectile/EnemyAI 均用新签名）。期间发现 MyScripts/PlayerAttack.cs 缺第 6 步判定球生成代码（J 键近战不掉血），已提醒，用户已自行补充正确（AttackRoutine 末尾 new GameObject("AttackHit") + SphereCollider isTrigger radius0.6 + 传 damage/owner）。待用户验证后进入第 10 步 UI 血条+技能按钮（HealthBarUI.cs）。

## 第 10 步（UI 血条+技能按钮）已讲解
- HealthBarUI.cs 已移入 AIScripts（现 10 个脚本），暂存目录只剩 GameManager.cs（第 11 步）。
- 内容：HealthBarUI 订阅 Health.OnDamaged 事件刷新 Image.fillAmount（UI 与逻辑解耦，OnEnable 订阅/OnDisable 退订）；技能按钮绑定 SkillController.ReleaseSkill(0)；可选冷却倒计时显示（读取 Skill.lastUsedTime）。
- 下一步（第 11 步，最后一步）：胜负判定与重置 GameManager.cs（订阅双方 OnDeath 显示胜负文本，按 R 重置，简单单例 Instance）。

## 第 11 步（胜负判定与重置）已讲解——TestDemo 11 步全部讲完
- GameManager.cs 已移入 AIScripts，暂存目录 `.codebuddy\testdemo_reference\AIScripts\` 已清空，11 个参考脚本全部就位（CameraFollow/PlayerController/PlayerAttack/Health/AttackHit/Skill/SkillController/Projectile/EnemyAI/HealthBarUI/GameManager）。
- 第 11 步要点：简单单例 Instance、Awake 用 lambda 订阅双方 OnDeath（player 死→"你输了"/enemy 死→"你赢了"）、出生点快照 playerStart/enemyStart、ResetGame 用 Health.Restore()（第 9 步已实现）。指南 code 段把 ResetGame 留成"自己实现"引导用户自己写。
- 指南第四章含 6 个扩展方向（技能冷却进阶/受击粒子/伤害数字/音效/摄像机震屏/连招等）。
- 待用户完成第 11 步验证后，TestDemo 学习项目基础版全部完成。
