# MyprojectDemo（UFE 格斗游戏）零基础完整学习路径

> 本学习路径将你当作**完全零基础**的初学者来设计：从"什么是 Unity"开始，一路学到"能看懂并修改这个格斗游戏项目"。
> 全程以 **MyprojectDemo** 项目（Unity 2020.3.17f1 + UFE 2.x 格斗引擎）为载体，边看边学、边改边玩。
> 预计总投入：每天 2~3 小时，约 3~4 周可完整走通。

---

## 学习路线总览（先看这张地图）

```
[阶段0] 明确目标与心态
   ↓
[阶段1] Unity 基础扫盲 ──────────► 会打开项目、能运行游戏
   ↓
[阶段2] C# 语言基础 ─────────────► 能读懂项目里的代码
   ↓
[阶段3] 玩透这个 Demo ───────────► 亲手体验所有模式与按键
   ↓
[阶段4] UFE 架构总览 ────────────► 看懂"这个引擎怎么组织起来的"
   ↓
[阶段5] 输入系统 ────────────────► 弄懂"按键是怎么进到游戏里的"
   ↓
[阶段6] 战斗物理核心 ────────────► 弄懂"角色怎么打起来、受伤的"
   ↓
[阶段7] UI 界面系统 ─────────────► 弄懂"菜单/血条/结算怎么运作"
   ↓
[阶段8] AI 系统 ─────────────────► 弄懂"电脑对手怎么思考"
   ↓
[阶段9] 动画系统 ────────────────► 弄懂"角色怎么动起来的"
   ↓
[阶段10] 动手改项目 ─────────────► 改键位/改数值/建角色 → 做出自己的格斗游戏
   ↓
[阶段11] 进阶深挖 ───────────────► 兼容层/网络/编辑器工具/性能
   ↓
[阶段12] 项目笔记与 FAQ ─────────► 输出自己的知识体系
```

---

# 阶段 0：明确目标与心态（第 1 天·30 分钟）

## 0.1 你要学的是什么

MyprojectDemo 不是普通的小游戏，它是一个**工业级格斗游戏引擎（UFE）的完整演示**。通过它你能学到：

| 能力 | 具体内容 |
|---|---|
| Unity 基础 | 场景、预制体、组件、资源管理 |
| C# 面向对象 | 类、继承、多态、接口、委托、协程 |
| 游戏架构 | 全局管理器、数据驱动设计、状态机、事件系统 |
| 格斗游戏专有 | 输入缓冲、出招表识别、HitBox 判定、帧数据、连招系统 |
| UI 框架 | 界面生命周期、导航系统、UI 与逻辑分离 |
| 编辑器工具 | 用编辑器窗口配置角色/招式（可视化配置） |

## 0.2 需要准备的工具

1. **Unity Hub + Unity 2020.3.17f1**（版本必须匹配，`ProjectSettings/ProjectVersion.txt` 里写明了版本）
2. **IDE**：Visual Studio 或 VS Code（配合 C# 插件），用来读/写代码
3. **本项目的文件管理器**：随时看文件结构（推荐用 IDE 的资源管理器）
4. **一本好的 C# 教程**（见阶段 2 推荐）

## 0.3 学习心态建议

- **先跑通，再读码**：先把游戏跑起来玩几天，建立"这游戏是什么"的直觉，再去看代码。
- **配置驱动一切**：这个项目里大部分"游戏内容"（角色、招式、UI、回合数）都不在代码里，而在 `.asset` 配置文件中。看懂配置 = 看懂一半项目。
- **读代码不求一次全懂**：核心文件（如 `UFE.cs` 有 2000+ 行）可以跳着读，先抓主线。
- **每阶段必须有输出**：每个阶段结束时，写一份自己的笔记或做一个改动，才算真正学会。

---

# 阶段 1：Unity 基础扫盲（第 1~2 天）

> 目标：理解 Unity 的核心概念，能打开项目、运行游戏、在场景里"逛一逛"。

## 1.1 Unity 是什么

Unity 是一个游戏引擎（工具），核心工作方式：**把资源（模型/图片/声音）拖进场景，在物体上挂组件（Component）赋予功能，用 C# 脚本写逻辑，然后点 Play 运行。**

记住三个核心概念：

```
场景(Scene)     = 一个游戏关卡/界面（本项目的场景：TrainingRoom.unity）
GameObject(物体) = 场景里的一个东西（角色、相机、灯光都是物体）
组件(Component) = 挂到物体上的功能模块（Transform、Camera、脚本...）
脚本(Script)    = 用 C# 写的自定义组件
```

## 1.2 Unity 编辑器界面（打开后先认识这 5 个窗口）

| 窗口 | 作用 |
|---|---|
| **Hierarchy（层级）** | 列出当前场景所有物体（左上） |
| **Scene（场景）** | 可视化编辑场景（中间） |
| **Game（游戏）** | 运行时看到的画面 |
| **Inspector（检查器）** | 查看/修改选中物体或资产的属性（右侧） |
| **Project（项目）** | 项目所有文件资产（下方） |

## 1.3 本项目的 Unity 操作练习（照着做）

1. 用 Unity Hub 打开 `g:\MyprojectDemo`（首次打开会导入资源、编译，等待完成）。
2. 打开场景：`File → Open Scene → Assets/UFE/Demo/TrainingRoom.unity`。
3. 在 **Hierarchy** 里点击 `UFE Manager`，在 **Inspector** 里看到挂着的 `UFE.cs` 脚本和 `EventSystem` 组件。
4. 点击 **Scene** 窗口，用鼠标中键拖动、滚轮缩放，观察场景里的相机和物体。
5. 点击 **▶ Play**，看到游戏跑起来（片头→主菜单）。用 `WASD` 移动、`T` 确认操作几下。再按 **■** 停止。
6. 在 **Project** 窗口按路径浏览：
   - `Assets/UFE/Demo/` → 演示内容（角色、GUI、场景）
   - `Assets/UFE/Engine/Scripts/` → 引擎代码
   - `ProjectSettings/InputManager.asset` → 按键配置（可在 Inspector 查看）

## 1.4 必须掌握的小概念

- **Prefab（预制体）**：做好的"模板物体"，可反复实例化。本项目所有界面都是预制体（`Demo/GUI/UI_Prefabs/`）。
- **Asset（资产文件）**：素材文件。本项目大量使用 **ScriptableObject 资产**（`.asset`）存游戏数据——这是后面学习的重点。
- **Transform**：每个物体都有的位置/旋转/缩放组件。
- **Camera**：场景里的"眼睛"。

## 1.5 验证学习成果

- [ ] 能说出 Scene/Hierarchy/Inspector/Project 的作用
- [ ] 能在不看书的情况下打开场景并运行游戏
- [ ] 能在项目里找到：入口场景、Config 配置、GUI 预制体目录、核心脚本目录

---

# 阶段 2：C# 语言基础（第 3~5 天）

> 目标：能读懂项目里的代码。不需要成为语言专家，只需要掌握"读代码"需要的 80% 语法。

> 推荐教程（任选）：微软官方《C# 编程指南》、菜鸟教程 C#、B 站 C# 入门教程。学到"面向对象"即可，不用学 WinForm/WPF 等桌面开发内容。

## 2.1 按优先级掌握的知识点

| 优先级 | 知识点 | 在本项目哪里用到了 |
|---|---|---|
| ★★★ | 变量、类型（int/float/bool/string/枚举） | 到处都是，如 `ButtonPress` 枚举 |
| ★★★ | 方法（函数）、参数、返回值 | 每个脚本都是 |
| ★★★ | `class`（类）、`public/private/protected` | 每个脚本都是 |
| ★★★ | 继承（`class A : B`）、`virtual`/`override` | **UI 系统的核心**：`DefaultMainMenuScreen : MainMenuScreen : UFEScreen` |
| ★★★ | `using` 命名空间、using 别名 | 兼容层大量使用 `using X = XCompat;` |
| ★★★ | `MonoBehaviour` 生命周期（`Awake/Start/Update/OnEnable`） | 每个 Unity 脚本 |
| ★★☆ | 接口 `interface` | `MultiplayerAPI`、`IInput` 等 |
| ★★☆ | 委托 `delegate`、事件、`Action`/`Func` | `UFE.OnButton`、`onClick.AddListener` |
| ★★☆ | 协程 `IEnumerator`/`yield`/`StartCoroutine` | `TextureIntroScreen` 跳过片头 |
| ★★☆ | 泛型 `List<T>`、字典 `Dictionary<K,V>` | 输入缓冲、角色列表 |
| ★☆☆ | `static` 静态成员 | `UFE` 类几乎所有方法都是静态的 |
| ★☆☆ | 属性（`get`/`set`）、自动属性 | `UFE.config`、`CharacterInfo` 字段 |

## 2.2 在本项目里做 C# 阅读练习

打开这些文件（由易到难），试着说出每一行在干什么：

1. `Assets/UFE/Engine/Scripts/UI/Base/UFEScreen.cs`（不到 30 行）——最友好的入门文件，看类继承与方法。
2. `Assets/UFE/Engine/Scripts/UI/Base/MainMenuScreen.cs`——看方法如何调用其他类（`UFE.StartStoryMode()`）。
3. `Assets/UFE/Engine/Scripts/Input/InputEvents.cs`——看构造方法重载。
4. `Assets/UFE/Engine/Scripts/Definitions/MoveInfo.cs` 开头——看枚举定义。
5. `Assets/UFE/Engine/Scripts/UI/Templates/DefaultMainMenuScreen.cs`——综合练习：继承、字段、方法重写、回调。

> 读代码技巧：**从方法名猜用途 → 从调用处反推 → 用 IDE 的"转到定义"（F12）追踪**。

## 2.3 验证学习成果

- [ ] 能解释 `DefaultMainMenuScreen : MainMenuScreen : UFEScreen` 这条继承链
- [ ] 能区分 `public` 和 `private` 并说明理由
- [ ] 能说出一段协程代码的执行顺序
- [ ] 能在项目里找一个 `Dictionary` 和 `List` 的用法例子

---

# 阶段 3：玩透这个 Demo（第 5~6 天）

> 目标：亲手玩遍游戏的所有功能，建立"游戏体验 → 代码实现"的对应感。这一步很关键——只有知道游戏是什么样，才能读懂代码。

## 3.1 把游戏每个模式都玩一遍

按下面的清单操作并记录你的感受：

- [ ] **片头**：按任意键跳过 vs 等待 3 秒自动进入
- [ ] **主菜单**：8 个按钮各点一次，观察 Online/Bluetooth 是灰色的（为什么？见 10 节）
- [ ] **Versus → Player vs CPU**：选择 **Ethan**，进入战斗，用 `T/Y/U/G/H/J` 打一局
- [ ] **出必杀技**：`S→D` + 拳 = 波动拳；`D→S→D` + 拳 = 升龙拳（多试几次感受手感）
- [ ] **防守**：按住 `A`（后方向）格挡
- [ ] **暂停**：按 `Enter`，进入暂停菜单，打开 **Special Moves** 看内置出招表，打开 **Controls** 看内置键位表
- [ ] **训练模式**：主菜单 → Training Mode，感受"生命自动回血、不会死"
- [ ] **双人对战**：Versus → Player vs Player，P1 键盘左侧 / P2 键盘右侧
- [ ] **CPU vs CPU**：选好角色后看电脑自己打
- [ ] **结算界面**：打完一局后点 Repeat Battle / Character Selection / Stage Selection / Main Menu
- [ ] **故事模式**：Story Mode，体验连续战斗流程

## 3.2 带着问题玩

玩的过程中记下这些问题，后面阶段逐一解答：

1. 游戏一开始"谁"负责显示片头？（→ 阶段 4：`UFE.cs` 的 `Awake`）
2. 按键是怎么从键盘传到角色动作的？（→ 阶段 5：输入系统）
3. 为什么"后方向"就是格挡？为什么出招要"方向+按键"组合？（→ 阶段 6：招式识别）
4. 血条、能量槽、回合提示这些 UI 是怎么更新的？（→ 阶段 7）
5. 电脑对手为什么会有强有弱？（→ 阶段 8：AI 难度）

## 3.3 验证学习成果

- [ ] 能不看文档说出所有模式在哪进入
- [ ] 能说出 P1/P2 全套键位
- [ ] 能流畅发出波动拳和升龙拳
- [ ] 能说出游戏中至少 3 个"你看不懂但想知道原理"的现象

---

# 阶段 4：UFE 架构总览（第 6~7 天）

> 目标：理解这个引擎的"骨架"——它是怎么组织 200 多个脚本的。

## 4.1 UFE 的分层思想（最重要的认知）

UFE 采用了**分层 + 数据驱动**架构：

```
┌─────────────────────────────────────────────┐
│  内容层（Content）                           │  ← 做游戏的人改这里
│  Demo/ 下的角色资产、GUI预制体、Config.asset │
├─────────────────────────────────────────────┤
│  模板层（Template）                         │  ← 半成品实现
│  UI/Templates/Default*Screen.cs             │
├─────────────────────────────────────────────┤
│  基类层（Base / Abstract）                  │  ← 抽象接口
│  UI/Base/UFEScreen.cs、Input/Abstract*.cs   │
├─────────────────────────────────────────────┤
│  引擎核心（Manager/Physics/...）            │  ← 不要轻易改
│  Manager/UFE.cs、Physics/ControlsScript.cs  │
└─────────────────────────────────────────────┘
```

**核心思想：游戏逻辑写在代码里，游戏内容写在配置里。** 想改"有多少个角色、角色的血是多少、出招表是什么"，不需要改代码，改 `.asset` 配置即可（这也是阶段 10 你能动手改的基础）。

## 4.2 目录地图（对照阅读）

| 目录 | 内容 | 重要程度 |
|---|---|---|
| `Scripts/Manager/UFE.cs` | **全局大脑**：启动流程、屏幕切换、战斗调度 | ★★★★★ |
| `Scripts/Definitions/` | 配置类：GlobalInfo/CharacterInfo/MoveInfo/StanceInfo | ★★★★★ |
| `Scripts/Physics/` | 战斗物理：ControlsScript/MoveSetScript/HitBoxesScript | ★★★★★ |
| `Scripts/Input/` | 输入：InputController/InputEvents/UFEController | ★★★★☆ |
| `Scripts/UI/` | 界面：Base（抽象）+ Templates（实现） | ★★★★☆ |
| `Scripts/AI/` | 电脑 AI：Fuzzy 模糊逻辑引擎 | ★★★☆☆ |
| `Scripts/Animation/` | 动画控制：Mecanim/Legacy | ★★★☆☆ |
| `Scripts/Network/` | 网络（旧 UNET，已被 Unity 移除，当前不可用） | ★☆☆☆☆ |
| `Scripts/FPLibrary/` | 定点数数学库（Fix64/FPLibrary）——格斗游戏为保证联网同步使用定点数而非 float | ★★☆☆☆ |
| `Editor/` | 编辑器工具：UFE 配置窗口（改角色/招式用） | ★★★☆☆ |

## 4.3 精读 `UFE.cs` 的主线（分三次读，不要求一次读全）

**第一次：找启动流程**
- 打开 `Assets/UFE/Engine/Scripts/Manager/UFE.cs`
- 定位 `Awake()`（约 2100 行附近），读这一小段：
  ```csharp
  if (UFE.config.debugOptions.startGameImmediately){ ... }else{ UFE.StartIntroScreen(0f); }
  ```
  → 这就是"启动后进片头"的决定处。
- 再定位 `_StartIntroScreen` → `_StartMainMenuScreen`，理解"屏幕切换"套路：
  **隐藏当前屏幕 → 显示目标屏幕 → 淡入淡出**。

**第二次：找屏幕切换 API**
- 搜索 `StartMainMenuScreen` / `StartVersusModeScreen` / `StartStoryMode` / `StartTrainingMode` / `StartPlayerVersusCpu`
- 你会发现它们都是**静态方法**，内部调用 `_StartXxx(fadeTime)` 执行实际切换。这就是全局管理器的典型写法。

**第三次：找战斗调度**
- 搜索 `_StartGame`、`OnRoundEnds`、`OnGameEnds`
- 理解：战斗结束 → 判断胜负 → 显示对应结算界面。

> 读大文件的技巧：用 IDE 的"大纲/符号列表"功能（VS 里是 View → Object Browser 或右侧符号栏），先看全部方法名，再挑重点读。

## 4.4 认识"配置即内容"

1. 在 Project 窗口选中 `Assets/UFE/Demo/Config.asset`，看 Inspector。
2. 依次展开：`Game GUI`（所有界面预制体引用）、`Round Options`（回合数=3、计时=99）、`AI Options`（难度=2）、`Training Mode Options`、`Debug Options`（startGameImmediately=0）。
3. 选中 `Assets/UFE/Demo/Characters/Ethan/Ethan.asset`，看 Inspector：`lifePoints: 1100`、`maxGaugePoints: 1000`、招式列表。
4. **动手练习**：把 `Config.asset → Debug Options → startGameImmediately` 勾上，Play 看看会发生什么（会跳过片头直接进战斗）。玩完记得改回来。

## 4.5 验证学习成果

- [ ] 能画出 UFE 的四层架构图
- [ ] 能说出 `UFE.cs` 里"启动流程"和"屏幕切换"分别在哪个方法
- [ ] 能说出修改"回合数""计时""AI 难度"分别改哪个配置
- [ ] 能解释"内容层与代码层分离"带来的好处

---

# 阶段 5：输入系统（第 7~8 天）

> 目标：完整回答"我按下 T 键 → 角色打出轻拳"中间发生了什么。

## 5.1 数据流总览

```
键盘按键
   ↓ (1) 读取
ProjectSettings/InputManager.asset  ← Unity 输入管理器（轴定义：P1Button1 = 键 T）
   ↓ (2) 轮询
InputController.cs（ReadInput()：Input.GetButton("P1Button1")）
   ↓ (3) 打包
InputEvents（含 axisRaw 轴值 + button 布尔）
   ↓ (4) 分发
ControlsScript.cs（DoFixedUpdate(previousInputs, currentInputs)）
   ↓ (5) 映射
engineRelatedButton → ButtonPress 枚举（Button1=4 轻拳 / Button2=5 中拳 ...）
   ↓ (6) 触发
招式系统/攻击判定
```

## 5.2 精读清单（按顺序）

1. **`ProjectSettings/InputManager.asset`**：Unity 的输入轴表。看 `P1KeyboardHorizontal`（`a`/`d`）、`P1Button1`（`t`）、`P2Start`（`space`）等定义。
   - 关键概念：轴（Axis）= 连续值（如水平移动），按钮（Button）= 是/否（如出拳）。
2. **`InputEvents.cs`**：理解"一帧输入"的数据结构（`axisRaw` 与 `button` 两个字段）。
3. **`InputController.cs`**：
   - `ReadInput()`：怎么从 Unity 读到值；
   - `InitializeInput()` / `SelectInputType()`：选择输入实现（默认 Unity 输入管理器）。
4. **`ControlsScript.cs` 中的 `translateInputs()`**（约 727 行起）：
   - 轴 → 方向（Forward/Back/Up/Down）；
   - 两个轴同按 → 生成斜方向（DownForward 等）；
   - 按钮按下/松开判定（`buttonDown` = 本帧按下且上帧未按）。
5. **`Definitions/MoveInfo.cs` 里的 `ButtonPress` 枚举**：记住数值：
   ```
   0 Forward(前) 1 Back(后) 2 Up(上) 3 Down(下)
   4 Button1(轻拳) 5 Button2(中拳) 6 Button3(重拳)
   7 Button4(轻脚) 8 Button5(中脚) 9 Button6(重脚)
   16 Start
   ```

## 5.3 动手实验

- **实验 1**：在 `InputManager.asset` 里把 `P1Button1` 的 `positiveButton` 从 `t` 改成 `q`，Play 试试（轻拳变成 Q 键）。理解"配置驱动输入"。
- **实验 2**：在 `UFE_Config.asset` 的 `player1_Inputs` 里找到 `P1Button1` 行，看 `engineRelatedButton: 4`（=Button1），理解"输入项 → 引擎动作"的映射。

## 5.4 验证学习成果

- [ ] 能画出"按键 → 角色动作"的完整数据流（含每一步的文件）
- [ ] 能说清 `axisRaw` 和 `button` 的区别
- [ ] 能说出 `ButtonPress` 枚举 0~9 和 16 的含义
- [ ] 能自己改一个键位并解释原理

---

# 阶段 6：战斗物理核心（第 8~11 天）

> 目标：理解"角色怎么移动、出招、被命中、掉血、倒下"。这是格斗引擎最核心的部分，**值得多花时间**。

> 提前概念：格斗游戏本质是**有限状态机（FSM）+ 帧数据**。角色永远处于某个"状态"（站立/出拳/受击/倒地），每个招式有"帧数据"（前摇几帧、持续几帧、后摇几帧）。

## 6.1 核心文件地图

| 文件 | 职责 |
|---|---|
| `Physics/ControlsScript.cs` | 角色的"身体"：状态机、移动、生命、能量、按钮输入、伤害结算（140KB，最大文件） |
| `Physics/MoveSetScript.cs` | 角色的"招式表"：招式匹配、出招识别（必杀技/连招）、AI 协助 |
| `Physics/HitBoxesScript.cs` | 判定盒：攻击判定框、受击判定框、防御框 |
| `Physics/PhysicsScript.cs` | 物理移动（重力、跳跃、地面） |
| `Physics/ProjectileMoveScript.cs` | 飞行道具（波动拳气功弹） |

## 6.2 推荐精读顺序（从易到难）

1. **先读 `Definitions/CharacterInfo.cs`**：认识角色数据结构（名字、生命、能量、招式列表、动画）。
2. **再读 `Definitions/MoveInfo.cs`**：认识"一个招式"的数据结构——这是出招识别的关键：
   - `basicMoves`：基础动作（站立/行走/跳跃/普通拳脚）；
   - `linkMoves` / `chainMoves`：连段；
   - `defaultInputs`：必杀技的输入指令（如波动拳 = `Down, Forward, Punch`）；
   - `MoveInput` 里 `buttonSequence` = 按钮序列，`buttonExecution` = 按钮执行方式。
3. **读 `MoveSetScript.cs` 的招式匹配部分**：搜索 `GetMove`、`GetCharacterMove`，理解"我输入的序列和招式表比对了没有"。
4. **读 `ControlsScript.cs` 的主线**（挑重点方法）：
   - `DoFixedUpdate`：每帧干的事；
   - `translateInputs`：输入转换成方向/按钮；
   - `CheckHitBoxes` / `TakeDamage`：命中与受伤；
   - `GetHit`：被打中的反应（硬直/击退）；
   - `Blocking` / `CheckBlocking`：防守。
5. **读 `HitBoxesScript.cs`**：理解判定盒怎么绑定到身体部位、怎么启用/禁用。

## 6.3 结合数据看源码（用 Ethan 的招式资产）

打开 `Assets/UFE/Demo/Characters/Ethan/Moves/Stand_DF1.asset`（波动拳），找到 `defaultInputs`：
```yaml
buttonSequence: 0300000000000000   # 3 = Down（下）
buttonExecution: 04000000           # 4 = Button1（轻拳）
```
- 含义：**先按"下"，再按"轻拳" = 波动拳**。
- 对照 `Stand_DF1DF1.asset`（超必杀波动）：`buttonSequence: 0300000000000000` + `buttonExecution: 04000000`，`previousMoves` 里引用了 `Stand_DF1`——表示**必须先发过波动拳，再在下个指令窗口内再次输入，才触发超必杀**。
- 这就是格斗游戏"简化指令"（如 `↓↘→` 简化为 `↓→`）的实现方式。

## 6.4 核心概念精讲

### 6.4.1 有限状态机（FSM）
角色状态：`Idle → Attack → Hit → Down → GetUp → Idle`。
在 `ControlsScript` 里用枚举（如 `currentState`）表示，根据输入/事件跳转。**这是所有格斗游戏的基础**。

### 6.4.2 帧数据（Frame Data）
每个动作分三段：
```
前摇(Startup) ──► 判定生效(Active) ──► 后摇(Recovery)
```
前摇短的先出手（强），后摇短的收招快（强）。在动画资产和 `animMap` 中体现（哪一帧哪个 HitBox 生效）。

### 6.4.3 判定盒（HitBox）
`HitBoxesScript` 定义攻击框（打人用）和受击框（被打用）。攻击框碰到对方受击框 → 命中结算。这也是角色选择界面里 `hitBox.position.gameObject.SetActive(hitBox.defaultVisibility)` 的用途（显示判定框调试）。

## 6.5 动手实验（训练模式调试）

1. 进入 **Training Mode**，选 Ethan 对练。
2. 打开 `Config.asset → Debug Options → debugMode` 勾选（true），Play 进入训练模式，屏幕会显示 FPS、生命、状态等调试信息——**这是理解状态机的最佳工具**。
3. 试试发波动拳的同时观察调试文本，看 `currentMove`/`currentState` 的变化。
4. （进阶）打开 `CharacterEditorWindow`（Unity 菜单 `Window → UFE`），看一个招式的帧数据配置界面。

## 6.6 验证学习成果

- [ ] 能画出角色一局内的状态机流转
- [ ] 能说出"前摇/判定/后摇"的含义
- [ ] 能解释波动拳资产里 `buttonSequence` 和 `buttonExecution` 的含义
- [ ] 能解释 HitBox 攻击框与受击框的判定关系
- [ ] 能在训练模式里打开调试信息并看懂 3 个字段

---

# 阶段 7：UI 界面系统（第 11~13 天）

> 目标：理解"从主菜单到结算，界面是怎么切换的"，以及"UI 数据是怎么和游戏数据绑定的"。

## 7.1 UI 三层架构（再次强调，这是看懂 UI 的关键）

```
继承链： DefaultMainMenuScreen（模板层，具体实现）
            ↓ 继承
        MainMenuScreen（基类层，抽象动作：GoToStoryMode 等）
            ↓ 继承
        UFEScreen（最底层：DoFixedUpdate/OnShow/OnHide）
```
- **基类层**（`UI/Base/`）：定义"这个界面能做什么"（方法）。
- **模板层**（`UI/Templates/`）：实现"具体怎么做"（字段 + 逻辑）。
- **预制体层**（`Demo/GUI/UI_Prefabs/`）：把模板脚本挂到 UI 物体上（按钮、图片、文字）。
- **配置层**（`Config.asset → gameGUI`）：决定"哪个界面用哪个预制体"。

## 7.2 精读清单

1. **`UI/Base/UFEScreen.cs`**：所有界面的爸爸。记住 4 个核心方法：
   - `DoFixedUpdate(...)`：每帧接收两个玩家的输入；
   - `OnShow()` / `OnHide()`：显示/隐藏时触发；
   - `IsVisible()`：是否可见。
2. **`UI/Templates/UFEScreenExtensions.cs`**：界面导航系统（重中之重）：
   - `DefaultNavigationSystem`：通用的"移动光标 + 确认 + 取消"导航；
   - `SpecialNavigationSystem`：特殊导航（角色选择时双人独立操作）；
   - `HighlightOption` / `MoveCursor`：光标移动与高亮。
   - **注意**：这里确认键 = `confirmButton`（Config 里 = Button1 = T 键），取消键 = `cancelButton`（= Button4 = G 键）。
3. **`UI/Templates/DefaultCharacterSelectionScreen.cs`**：最复杂的界面，看它如何：
   - 双人各自导航（PvP 时两套输入同时生效）；
   - 显示角色大立绘/名字；
   - `TrySelectCharacter` / `TryDeselectCharacter`；
   - 双方都选好 → 自动进入下一界面。
4. **`UI/Templates/DefaultBattleGUI.cs`**：战斗 HUD。看它如何在 `DoFixedUpdate` 里更新血条（`targetLife` 渐变）、计时器、回合提示。
5. **`UI/Templates/DefaultPauseScreen.cs`**：看多子界面切换（`screens` 数组 + `GoToScreen(index)`）和确认对话框。

## 7.3 界面切换流程（结合 UFE.cs）

```
UFE.StartMainMenuScreen()
  → 淡出当前界面（CameraFade）
  → _StartMainMenuScreen()
      → HideScreen(当前)
      → ShowScreen(主菜单预制体)
```
所有 `StartXxxScreen` 方法都是这个套路。理解这一个套路 = 理解所有界面切换。

## 7.4 动手实验

- 实验 1：在 `Config.asset → gameGUI → mainMenuScreen` 改引用为 `MainMenuScreenBT.prefab`（蓝牙版菜单），Play 观察差异。
- 实验 2：给主菜单加一个按钮？——先不急，阶段 10 再做。

## 7.5 验证学习成果

- [ ] 能画出 UI 的四层结构（基类/模板/预制体/配置）
- [ ] 能说清 `OnShow`/`OnHide`/`DoFixedUpdate` 的作用
- [ ] 能解释菜单里为什么按 `T` 是确认、按 `G` 是取消
- [ ] 能说出"屏幕切换"的标准套路
- [ ] 能解释角色选择界面如何支持双人独立操作

---

# 阶段 8：AI 系统（第 13~14 天）

> 目标：理解电脑对手是怎么"思考"的，以及为什么有难度之分。

## 8.1 核心认知

UFE 的 AI 是**模糊逻辑（Fuzzy Logic）**引擎，位于 `Scripts/AI/Fuzzy/`。它不做"完美操作"，而是模拟人类：
- 每隔一段时间（`timeBetweenDecisions`）做一次**决策**（前进/后退/攻击/防御）；
- 决策基于"评分制"：给每个可选动作打分，分数 = 权重 × 当前局面适配度；
- 难度越高 → 决策间隔越短、动作越精准、连招效率越高。

## 8.2 数据文件

- `Demo/AIInstructions/`：AI 指令资产（14 个），每个角色/招式对应一个 AI 行为配置文件。
- `Config.asset → aiOptions`：
  - `selectedDifficultyLevel: 2`（当前难度，范围 0~5）；
  - `difficultySettings[]`：6 档难度各自的行为参数（`timeBetweenDecisions`、`timeBetweenActions`、`comboEfficiency`）。

## 8.3 精读清单

1. 读 `Config.asset` 中 `aiOptions` 段，对比 6 档难度参数差异（重点看 `timeBetweenDecisions`：0.4→0.3→0→0.1→0→0，越小越强）。
2. 读 `Scripts/AI/Fuzzy/` 下的说明文档（`.md`/`.txt`），了解模糊 AI 原理。
3. （选读）看 `AIAsset.cs`（Editor 目录），了解 AI 指令资产的数据结构。

## 8.4 动手实验

- 把 `Config.asset → aiOptions → selectedDifficultyLevel` 改成 0（最弱）和 5（最强），各打一场 PvCPU，感受差异。
- 总结：难度本质上改的是什么？（答案：决策速度 + 行为效率）

## 8.5 验证学习成果

- [ ] 能说出 AI 的"决策间隔"和"连招效率"分别控制什么
- [ ] 能解释为什么难度 5 的 AI 更强
- [ ] 能在训练模式里观察 AI 的行为模式

---

# 阶段 9：动画系统（第 14~15 天）

> 目标：理解"角色模型怎么和游戏逻辑联动"。

## 9.1 两种动画系统并存

本项目同时存在两种动画方案（`Scripts/Animation/` 目录）：

| 类型 | 控制脚本 | 角色示例 |
|---|---|---|
| **Mecanim**（Animator 状态机） | `MecanimControl.cs` | Ethan、Mecanim Bot、Robot Kyle |
| **Legacy**（老式 Animation 播放） | `LegacyControl.cs` | Mike |

关键点：**游戏逻辑（ControlsScript）不直接碰动画**，而是通过一个中间层（`MecanimControl`/`LegacyControl`）把"状态变化"转成"播放对应动画片段"。

## 9.2 动画数据在哪儿

- 每个招式的动画片段（AnimationClip）由招式资产引用：`MoveInfo.animationClip`。
- 招式资产的 `animMap`：**逐帧映射表**——第 N 帧时判定盒在哪个位置、哪个身体部位。
- `MecanimAssets/` 下的 Animator Controller（`MC_Controller`）定义了动画状态机。

## 9.3 精读清单

1. 读 `Animation/MecanimControl.cs`：看它如何 `PlayAnimations`、如何通过 `animMap` 同步判定盒。
2. 读 `Animation/LegacyControl.cs`：对比 Legacy 播放方式。
3. 打开一个招式资产（如 `Ethan/Moves/Stand_N1.asset`）的 `animMap`，数一数有多少帧、每帧映射了什么。

## 9.4 动手实验

- 在 Unity 中双击 `Assets/UFE/Engine/Resources/MC_Controller.controller`，打开 Animator 窗口，观察状态机。
- 进入训练模式打一拳，切到 **Animation 窗口**观察当前播放的动画（与招式对应）。

## 9.5 验证学习成果

- [ ] 能说出 Mecanim 与 Legacy 动画的区别
- [ ] 能解释 `animMap` 的作用
- [ ] 能说出"游戏逻辑 → 动画"的桥接脚本是哪两个

---

# 阶段 10：动手改项目（第 15~20 天）

> 目标：把学到的知识用起来，做出自己的改动。**这是从"看懂"到"会做"的质变阶段。**
> 每完成一个小任务，都算一个里程碑。

## 10.1 第 1 周任务（零代码改动，纯配置）

| # | 任务 | 修改位置 | 验证方式 |
|---|---|---|---|
| 1 | 把回合数改成 5 | `Config.asset → roundOptions.totalRounds = 5` | 进对战看回合提示 |
| 2 | 把计时改成 60 秒 | `roundOptions.timer = 60` | 战斗中看倒计时 |
| 3 | 把 AI 难度改成 5 | `aiOptions.selectedDifficultyLevel = 5` | 打 PvCPU 感受变强 |
| 4 | 把 P1 轻拳改成 Q 键 | `InputManager.asset → P1Button1 → positiveButton = q` | 用 Q 出轻拳 |
| 5 | 把 Ethan 血量加到 2000 | `Ethan.asset → lifePoints = 2000` | 进游戏看血条变长 |
| 6 | 跳过片头直进战斗 | `Config.asset → debugOptions.startGameImmediately = true` | Play 直接开打（记得改回） |
| 7 | 把主菜单换成蓝牙版 | `gameGUI.mainMenuScreen → MainMenuScreenBT` | 看菜单变化 |

> 完成这些，你就理解了"**这个引擎的 80% 内容靠配置而非代码**"。

## 10.2 第 2 周任务（改代码，由易到难）

| # | 任务 | 思路提示 |
|---|---|---|
| 1 | **改游戏标题** | 找 `Config.asset → gameName`，或改主菜单预制体上的文字 |
| 2 | **给片头加音乐** | 看 `TextureIntroScreen.cs` 的 `OnShow`，在 `Config.asset → gameGUI.introScreen` 对应预制体上挂 `music` 字段（拖一个 `Demo/Sounds/` 下的音频） |
| 3 | **写一个自定义界面脚本** | 新建 `MyScreen.cs : UFEScreen`，在 `DoFixedUpdate` 里打印"我收到了玩家输入"；把 `Config.asset → gameGUI.mainMenuScreen` 指向一个挂了该脚本的测试预制体（注意备份原配置） |
| 4 | **加一个"重置位置"快捷键** | 在 `DefaultBattleGUI.cs` 或 `ControlsScript.cs` 的某处监听 `KeyCode.R`，调用重置逻辑 |
| 5 | **给 Ethan 加一个攻击力加成** | 找到 Ethan 某招式的伤害字段（`MoveInfo` 里），或直接在 `Config.asset`/招式资产里改伤害值 |
| 6 | **创建自己的新角色** | 复制 `Demo/Characters/Ethan/` 整个文件夹 → 改名 → 修改 `.asset` 里的 `characterName`、`lifePoints` → 把新角色资产加进 `Config.asset → characters` 列表 → 进入选人界面看是否出现 |

> 每个任务做完，都建议用 git 提交一次（如果项目有 git），方便回退。

## 10.3 大任务（可选进阶）：理解并扩展"兼容层"

- 读 `Assets/UFE Addons/Compatibility/UFELegacyAPICompat.cs`，理解：
  - 为什么需要它？（Unity 2018.2 移除了 GUIText/旧 UNET/MovieTexture）
  - 它是怎么做到"不改业务代码"的？（`using GUIText = GUITextCompat;` 别名替换）
- 这个兼容层本身就是很好的"适配器模式"教学案例。

## 10.4 验证学习成果

- [ ] 完成第 1 周 7 个配置任务并理解每个的原理
- [ ] 完成第 2 周至少 4 个代码任务
- [ ] 能说出"这个引擎改内容与改逻辑分别改哪里"

---

# 阶段 11：进阶深挖（第 20~25 天，可选）

> 目标：向"专家"方向前进。以下内容按兴趣选择。

## 11.1 定点数与网络同步（FPLibrary）

- 读 `Scripts/FPLibrary/`：为什么格斗游戏要用定点数（`Fix64`）而不是 `float`？
  - 答案：不同机器浮点运算结果可能不同，导致"同样的输入，结果不同"（不同步）。格斗游戏是**确定性的（deterministic）**，必须保证所有机器计算结果完全一致。
- 读 `Config.asset` 里 `_serializedValue` 这类字段，理解定点数如何序列化。

## 11.2 网络架构（当前不可用但可学）

- 读 `Scripts/Network/`：UFE 的旧 UNET 多层网络架构（服务器-客户端、帧同步）。
- 读 `UFE.cs` 里 `SearchClass("UnetHighLevelMultiplayerAPI")` 的反射加载逻辑：**为什么插件没了，项目还能编译**——反射 + 可选降级的设计。
- 结合兼容层 `NetworkCompat` 理解"API 演化下的兼容策略"。

## 11.3 编辑器工具（UFE 配置编辑器）

- 打开 Unity 菜单 `Window → UFE`，熟悉它的角色/招式编辑器。
- 读 `Editor/CharacterEditorWindow.cs`、`MoveEditorWindow.cs`、`GlobalEditorWindow.cs`，理解：
  - `ScriptableObject` 资产如何被编辑器读写；
  - 自定义编辑器窗口（`EditorWindow`）与 `CustomEditor` 的工作原理；
  - 这是 Unity 编辑器扩展开发（很值钱的能力）的完整范例。

## 11.4 状态机与帧数据的深化

- 把 `ControlsScript.cs` 里的状态枚举全部找出来，画一张完整的状态转换图。
- 研究 `executionBufferTime: 30`（输入缓冲）和 `plinkingDelay: 2`（双键连按窗口）——这两个参数是格斗游戏手感的核心。

## 11.5 性能与架构思维

- 观察 `Mono4Unity/` 目录（UFE 移植用的工具类库），了解第三方库在引擎项目中的组织方式。
- 思考：UFE 为什么把"引擎"和"内容"分成不同目录？如果让你设计一个游戏框架，你会怎么分层？

---

# 阶段 12：项目笔记与 FAQ（贯穿始终 + 第 25 天后收尾）

## 12.1 建议输出自己的项目笔记

```
你的笔记模板：
├─ 00 概念词典.md          # 记录所有新名词（状态机、帧数据、HitBox...）
├─ 01 启动流程.md          # 从 Play 到主菜单发生了什么
├─ 02 按键流程.md          # 从按键到动作的完整链路
├─ 03 战斗系统.md          # 状态机 + 判定 + 伤害
├─ 04 界面流程.md          # 每个界面的职责与切换
├─ 05 配置大全.md          # 每个 .asset 配置项的作用
├─ 06 我的改动.md          # 记录你做的每个实验和结果
└─ 07 疑问清单.md          # 记录还没搞懂的问题
```

## 12.2 高频 FAQ

**Q1：项目里的 Online/Bluetooth 按钮为什么是灰色的？**
因为旧版 UNET 网络系统被 Unity 2020.3 移除了，UFE 检测不到网络插件就自动降级，PC 端联机不可用。这是预期行为，不是 bug。

**Q2：为什么读代码时会看到 `using GUIText = GUITextCompat;`？**
这是兼容层做的"类型别名替换"，把旧 API 名指向自定义的兼容实现，从而不改动业务代码。想深入学习就去看 `UFELegacyAPICompat.cs`。

**Q3：`Config.asset` 和 `UFE_Config.asset` 有什么区别？**
- `Assets/UFE/UFE_Config.asset`：UFE 插件的全局默认配置（含输入绑定、功能开关）。
- `Assets/UFE/Demo/Config.asset`：**本项目实际使用的配置**（UFE.cs 在 Awake 时加载它），包含 GUI、回合、AI、调试等。
- 它们字段基本同构，Demo 版本是给这个演示项目用的。

**Q4：我想加一个全新角色，最简步骤是什么？**
1. 复制 `Ethan` 文件夹重命名；
2. 改 `.asset` 的 `characterName`、`lifePoints`、`prefabResourcePath`；
3. 把角色资产加入 `Config.asset → characters`；
4. 在 `CharacterSelectionScreen.prefab` 里加一个角色按钮（挂按钮引用）。
5. 进游戏选人界面验证。

**Q5：怎么最快看到"代码改动 → 游戏效果"？**
改完代码保存 → Unity 自动重新编译 → 点 Play。如果改了配置 `.asset`，直接在 Inspector 里改然后 Play 即可。注意：`InputManager.asset` 修改后某些情况需要重启编辑器才生效。

**Q6：为什么游戏要固定帧率？**
格斗游戏要求"确定性"——同样输入必须产生同样结果（否则联网同步会崩）。UFE 用固定时间步长（`UFE.fixedDeltaTime`）驱动逻辑，所以界面逻辑放在 `DoFixedUpdate` 而不是 `Update`。

**Q7：读 `UFE.cs` 这种几千行的文件有什么技巧？**
1. 用 IDE 的符号大纲先看全部方法名；
2. 先读公开的 `Start*` 方法（入口）；
3. 遇到不懂的字段先跳过，抓主线；
4. 结合 `Config.asset` 的字段对照读，往往能猜出含义。

## 12.3 最终检验：独立完成一个小项目

> 学完所有阶段后，试着**不借助本 Demo**，用 UFE 从零搭一个自己的格斗演示：
> 1. 新建空场景，挂 UFE 组件；
> 2. 复制 2 个角色资产并改名；
> 3. 建 1 个场地；
> 4. 配好 GUI 界面（复用 Demo 的预制体）；
> 5. 调成自己喜欢的手感（出招宽容度、伤害、节奏）；
> 6. Build 一个可执行文件。
> 能做到这些，你就真正"吃透"了这个项目。

---

## 附录 A：全部关键文件速查表

| 你想学什么 | 去读这些文件 |
|---|---|
| 启动流程 | `Scripts/Manager/UFE.cs`（Awake/StartIntroScreen/StartMainMenuScreen） |
| 屏幕切换 | `UFE.cs` 里所有 `Start*Screen` 方法 |
| 配置结构 | `Scripts/Definitions/GlobalInfo.cs` |
| 角色数据结构 | `Scripts/Definitions/CharacterInfo.cs` |
| 招式数据结构 | `Scripts/Definitions/MoveInfo.cs`（含 ButtonPress 枚举） |
| 按键→输入 | `ProjectSettings/InputManager.asset` + `Scripts/Input/InputController.cs` |
| 输入事件 | `Scripts/Input/InputEvents.cs` |
| 战斗逻辑 | `Scripts/Physics/ControlsScript.cs` |
| 招式识别 | `Scripts/Physics/MoveSetScript.cs` |
| 判定盒 | `Scripts/Physics/HitBoxesScript.cs` |
| 飞行道具 | `Scripts/Physics/ProjectileMoveScript.cs` |
| UI 基类 | `Scripts/UI/Base/UFEScreen.cs` |
| 菜单导航 | `Scripts/UI/Templates/UFEScreenExtensions.cs` |
| 战斗 HUD | `Scripts/UI/Templates/DefaultBattleGUI.cs` |
| 角色选择 | `Scripts/UI/Templates/DefaultCharacterSelectionScreen.cs` |
| 暂停菜单 | `Scripts/UI/Templates/DefaultPauseScreen.cs` |
| 动画控制 | `Scripts/Animation/MecanimControl.cs` / `LegacyControl.cs` |
| AI 配置 | `Demo/Config.asset → aiOptions` + `Scripts/AI/` |
| 兼容层 | `UFE Addons/Compatibility/UFELegacyAPICompat.cs` |
| 项目配置 | `Assets/UFE/Demo/Config.asset` |
| 全局配置 | `Assets/UFE/UFE_Config.asset` |

## 附录 B：推荐学习顺序时间表

| 周次 | 学习内容 | 目标 |
|---|---|---|
| 第 1 周 | 阶段 0~3 | 跑通游戏、建立基础、掌握操作 |
| 第 2 周 | 阶段 4~6 | 看懂架构、输入、战斗核心 |
| 第 3 周 | 阶段 7~9 | 看懂 UI、AI、动画 |
| 第 4 周 | 阶段 10 | 动手改项目（先配置后代码） |
| 第 5 周+ | 阶段 11~12 | 进阶深挖 + 独立小项目 |

---

> 最后一句忠告：**这个项目最大的价值是"一个完整的工业级游戏架构"**。哪怕你以后不做格斗游戏，这里的分层思想（配置驱动、引擎与内容分离、模板方法模式、状态机）都是通用的。把它吃透，你会获得远超"会做一个格斗游戏"的能力。
